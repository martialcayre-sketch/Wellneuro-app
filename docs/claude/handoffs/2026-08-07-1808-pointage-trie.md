# Handoff — 2026-08-07 — Le pointage trié : ce qui est clos quitte `next_action`

## Branche et état Git

- `claude/wellneuro-workflow-tokens-5x6jyy`, repartie d'`origin/main` (`892a5ff`).
- Documentation, prose d'état et deux commentaires de banc. Aucun code exécutable
  modifié, aucun fichier `web/src/`.

## Objectif

Achever ce que le LOT-01 (#575) avait laissé en réserve et que #612 n'avait fait
qu'à moitié : #612 a rendu `next_action` **fusionnable** (31 lignes au lieu
d'une) sans rien **trier**. Le champ mêlait toujours le clos et l'en-vol — c'est
ce mélange qui le fait grossir sans fin.

## Décisions prises

- **10 lignes sur 31 archivées**, vérifiées une à une contre le dépôt (PR
  mergées, `statut` des `CAMPAGNE.md`, fichiers cités) et non sur la foi du
  texte. `next_action` passe de 5 993 à 3 768 caractères (−37 %).
- **La ligne 27 a été reclassée par le dépôt lui-même** : « geste post-merge
  toujours dû — `--appliquer` réconcilie `git.*` » était classée « en vol » par
  l'investigation, mais #612 a supprimé le bloc `git` l'après-midi même. Elle
  était devenue sans objet entre l'analyse et l'exécution.
- **`wn-etat-reel.mjs` est gardé, ni branché ni supprimé.** Correction d'une
  formulation trop rapide de ma part : il n'est pas « orphelin d'appelants »,
  c'est un outil **à la main par conception** — `PROJET_CONTEXTE.md:109` pose
  déjà la frontière (« il rapporte », quand `--appliquer` répare) et son banc
  tourne en CI. Il manquait seulement dans les « Commandes utiles » de
  `CLAUDE.md`, d'où son invisibilité. Le brancher dans un skill coûterait un
  `gh` et un balayage de `web/src` à chaque invocation.
- **« `dirty` était toujours faux » corrigé en trois endroits** (un de plus que
  les deux signalés) : dans un texte qui parle d'un booléen nommé `dirty`,
  « faux » se lit comme la valeur, alors que la valeur stockée était toujours
  `true` — et donc toujours inexacte. Retour de la revue Copilot sur #612,
  publiée après le merge.

## Fichiers modifiés

`.wn/state.json` (`next_action` trié) · `CLAUDE.md` (commande + frontière
rapporter/réparer) · `scripts/wn-etat-reel.test.mjs` et `scripts/wn-cycle.test.mjs`
(commentaires) · `docs/claude/SESSION_LOG.md`.

## Validations exécutées

- **Aucune perte, prouvée** : les 10 lignes retirées de `next_action` sont
  toutes présentes dans l'archive ci-dessous, comparaison faite contre
  `892a5ff:.wn/state.json`. Les 21 gardées sont toutes issues de l'original.
- `node --test` sur les deux bancs : 48/48.
- `--appliquer` ne réécrit ni `next_action` ni de bloc `git`.
- `wn-github-orchestrator` rend la prochaine action réduite, sans erreur.
- `grep 'toujours faux'` hors archives : 0.

## Problèmes ouverts

- **`main` local ahead 50 / behind 51** — inchangé, arbitrage humain.
- Les quatre arbitrages de l'ancienne ligne 28 (PR #372, trois campagnes figées)
  restent dans `next_action` : ce sont des décisions, pas des faits clos.
- `wn-attendre-ci.mjs` ne propose pas de remède pour « aucun run créé ».

## Prochaine action exacte

Ouvrir la PR, lire `verify`, merger — en **attendant la fin de la revue
Copilot** cette fois : sur #612 elle a publié après le merge, et ses deux
observations étaient fondées.

## Interdits encore actifs

- Ne pas réintroduire de champ recalculable dans `.wn/state.json`.
- Pas de `pull`/`merge`/`rebase` automatique sur le `main` divergent.

## Archive — les 10 lignes closes retirées de `next_action`

Reprises mot pour mot. C'est ici qu'elles vivent désormais.

- LOT-00 LIVRÉ SUR SES DEUX MOITIÉS (2026-08-07).
- Moitié CODE mergée (PR #608) : bloc de retrait d'un instrument suspendu déjà présent dans un pack, dans la modale d'édition (PacksPanel.tsx:635-649, bâti sur l'instantané :69 et :187), GET /api/praticien/questionnaires qui sert `suspendus` à part des actifs (questionnaires/route.ts:32,48,68-72), et deux gardes posés APRÈS la revue — M1 (PacksPanel.test.tsx:314, l'instantané est réécrit à chaque ouverture, jamais accumulé) et M2 (:351-385, un suspendu ne compte pas comme un questionnaire envoyé).
- Moitié DONNÉE faite par le praticien via l'UI le 2026-08-07 à 15:46 (packs.updated_at = 2026-08-07 15:46:34.011), vérifiée par QUATRE lectures SQL du 2026-08-07 : (1) « Base de consultation » porte 5 qids — Q_MOD_03, Q_MOD_01, Q_INF_03, Q_SOM_09, Q_ALI_01 — IDENTIQUES QID POUR QID à web/prisma/seed.ts:270, donc le seed redevient le reflet de la production et la dérive du 2026-08-06 est refermée ; (2) pack_questionnaires du pack de base : 5 lignes, ordre [0,1,2,3,4], sans trou (syncPackToRegistry a purgé puis reconstruit) ; (3) SELECT ...
- WHERE 'Q_ALI_09' = ANY(qids) rend 0 ligne — il en rendait 1 depuis le 2026-08-06 18:02 ; (4) 1 seul pack actif, inchangé.
- AUCUN PATIENT N'A ÉTÉ TOUCHÉ : 0 assignation créée entre la dérive (2026-08-06 18:02) et le retrait (2026-08-07 15:46).
- Le risque d'auto-assignation de l'agenda sans décision praticien — ce que D-025 protège — est resté prospectif et ne s'est jamais réalisé ; il est fermé.
- Validations du LOT-00 (moitié code, PR #608) : T1 npm run check vert ; T3 npm run test:worktree vert en 1 min 47 s (Chromium + WebKit, build de production) ; check_no_secrets vert ; audit de campagnes vert ; revue adversariale wn-reviewer GO (5 mineurs corrigés).
- Trois abstentions confirmées sur pièce : packs/route.ts:307 acceptait déjà le retrait (diff `ajoutes` calculé contre l'existant), prisma/seed.ts:270 portait déjà 5 qids — la lecture d'après geste le confirme —, et le banc réclamé sur portail/valider existe déjà (valider/route.test.ts:103-115).
- Agenda alimentaire : 8/8 lots écrits livrés, LOT-06 seul en attente (voir ci-dessus).
- Geste post-merge toujours dû : node scripts/wn-cycle.mjs --appliquer depuis main (réconcilie git.*).
