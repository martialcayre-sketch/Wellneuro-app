# Handoff — 2026-09-20 — Le compteur de la panne WebKit est la PAGE (correction de D-233)

## 1. Branche et état Git

- Branche `wn-d233-compteur-de-page-2026-09-20`, partie d'`origin/main` à
  `f4512705`.
- **PR #1205 est ouverte en parallèle** (consignation du drapeau des assiettes).
  Les deux touchent `docs/claude/SESSION_LOG.md` : conflit d'**append** attendu
  au second merge, trivial — les deux entrées se gardent, dans l'ordre.
- **Aucun `D-NNN`** : une correction de fait, pas un arbitrage. Même forme que
  `#1187`, qui avait corrigé une prémisse fausse de `D-049` sans en prendre.

## 2. Objectif de la session

Corriger le mécanisme consigné par `D-233` : « création de contexte » là où la
mesure dit « création de page ».

## 3. Décisions prises

**Le compteur porte sur la création de PAGE.** Le bras C de la PR #1184 — un
contexte **unique**, une page **neuve** à chaque tour — bloque au **même rang
64** que le bras B. Le contexte est donc exclu comme compteur. Un test = une
page, d'où la signature « un seul test par run, jamais le même ».

**La clôture de `D-049` n'est pas rouverte**, et c'est le point à ne pas
confondre : la condition de sortie reste remplie, le correctif amont fonctionne,
les trois séquences T3 sont vertes. Seul le **mécanisme consigné** change.

**La correction est nommée, pas silencieuse.** `D-233` reçoit un encadré qui dit
ce qui était faux, ce qui ne l'est pas, et **pourquoi l'erreur a été commise**.

## 4. Ce que ce lot ferme, et ce qu'il n'atteint pas

- **Ferme** — sept énoncés vivants qui enseignaient le mauvais compteur, dont le
  message du classificateur d'échec E2E : celui-là s'adresse à quelqu'un en train
  d'instruire un rouge, c'est le plus coûteux à laisser faux.
- **N'atteint pas** — le `SESSION_LOG`, append-only : la correction s'écrit dans
  l'entrée du jour, elle ne se rétro-écrit pas.
- **N'atteint pas, et volontairement** — la description du **bras B** dans
  l'amendement du 2026-09-17 (« un contexte iPhone 13 neuf à chaque tour ») est
  **exacte** : c'est bien ce que ce bras faisait. Ce qui était faux est la
  conclusion qu'on en tirait. Sur-corriger l'aurait rendue fausse à son tour.

## 5. Fichiers modifiés

- `docs/DECISIONS.md` — l'encadré de correction sur `D-233`, son §2 et son §3, et
  le chapeau de clôture de `D-049` (deux occurrences).
- `.claude/rules/tests-validation.md` — la règle vivante.
- `scripts/wn-diagnostic-e2e.mjs` — le message du classificateur.
- `changelog.d/2026-09-17-cause-racine-d049.md` — **son titre**, qui contredisait
  sa propre table deux lignes plus bas.
- `changelog.d/2026-09-19-cloture-d049.md`, `docs/claude/SESSION_LOG.md`, ce
  handoff.

## 6. Validations exécutées

- `node --test scripts/wn-diagnostic-e2e.test.mjs` — **14/14** après réécriture
  du message.
- `node --test scripts/wn-coherence-etat.test.mjs` — 29/29.
- `node scripts/lib/decisions-numerotation.mjs` — OK, 237, sans trou.
- `node scripts/wn-ancres-doctrine.mjs` — OK, 4 ancres.
- **Balayage par le mot seul, pas par l'expression** : le premier `grep` sur
  « création de contexte » manquait les occurrences **coupées par un retour à la
  ligne**. Onze occurrences relevées, sept corrigées, quatre laissées exactes.
- T3 non rejoué : aucun fichier sous `web/`.

## 7. Problèmes ouverts

- **La cause de l'erreur est une habitude, pas un accident.** Troisième fois en
  trois jours qu'un constat de revue porte sur un fait affirmé sans avoir ouvert
  le fichier qui le définit — le `paths:` d'un fichier de règles, la condition de
  `cancel-in-progress`, les cinq motifs d'un frontmatter, et maintenant un
  mécanisme lu dans un **titre** plutôt que dans la table en dessous.
- Le témoin du drapeau des assiettes, toujours à venir (PR #1205).
- `Q_GAS_01` au socle : doctrine contre composition réelle, à trancher sur la
  production.
- Réserve préexistante du garde de paquet client.

## 8. Prochaine action exacte

1. Revue lue **aux trois emplacements**, puis merge `--squash --subject`.
2. Ordre conseillé : #1205 d'abord, puis celle-ci — le conflit d'append du
   `SESSION_LOG` se résout en gardant les deux entrées.

## 9. Interdits encore actifs

- Aucune identité réelle au dépôt ; dossiers de test lus par identifiant.
- Aucun secret en dur ; production en **lecture seule** par conteneur détaché.
- Pas de migration Prisma ni de `schema.prisma` sans demande explicite.
- Aucun geste de production sans autorisation explicite du responsable, pour ce
  geste-là.
- `retries` interdit à Playwright ; un rouge WebKit du CI ne se relance jamais.
- Force-push et arbitrages restent à demander. Autorisation Git courante jusqu'au
  **2026-09-24**.
