# Handoff — 2026-09-19 — D-049 est close (D-233)

## 1. Branche et état Git

- Branche `wn-cloture-d049-2026-09-19`, partie de `origin/main` à `006a0757`
  (`D-230`, mergée par une session voisine pendant la soirée).
- Écrite dans le checkout principal. `main` reste pris par le worktree verrouillé
  `correspondance-lot00`.
- **`D-233`, SECOND numéro de ce lot.** Écrit en `D-231` après un `fetch` qui
  donnait `D-230` ; le temps d'attendre le CI et l'arbitrage, une voisine a mergé
  `D-231` **et** `D-232`. Renuméroté au merge par substitution scriptée — 21
  occurrences, six fichiers. Relever après le `fetch` ne suffit pas : le numéro
  ne se réserve qu'au merge, et l'intervalle est la fenêtre de collision.
- **PR #1189 reste ouverte** (les deux lignes de règles) ; ce lot en est
  indépendant et part de `origin/main`, pas de sa branche.

## 2. Objectif de la session

Fermer `D-049` sur arbitrage du responsable du 2026-09-19, et faire le tour de ce
que la clôture déplace — ce qui n'est pas seulement une écriture de registre.

## 3. Décisions prises

**`D-233`** — `D-049` est close ; T3 local redevient exigé en entier, segment E2E
compris, pour les PR migration/scoring/clinique.

Trois choix dans ce lot méritent d'être nommés :

- **Un numéro neuf, pas un amendement en place.** L'arbitrage du 2026-09-19
  **renverse** celui du 2026-09-17. Un renversement se trouve, donc il se
  numérote : l'amender en place l'aurait rendu invisible à qui lit par la fin.
- **Le corps de `D-049` n'est pas réécrit.** Il porte l'instruction de la panne.
  Une décision close ne se réinterprète pas : elle reçoit un chapeau de clôture,
  et l'amendement du 2026-09-17 reçoit une mention de péremption de sa
  conclusion — ses constats de fait, eux, fondent `D-233`.
- **La règle du rouge WebKit du CI est redomiciliée, pas recopiée.** Amendement
  de `D-155` à `D-049`, elle devenait orpheline d'une décision close ; elle part
  dans `.claude/rules/tests-validation.md`.

## 4. Ce que ce lot ferme, et ce qu'il n'atteint pas

- **Ferme** — la dérogation E2E, vieille de 38 jours.
- **Ferme** — un classificateur qui annonçait « aucun correctif de notre côté
  n'est identifié » alors qu'il est en service depuis le 2026-09-17.
- **N'atteint pas** — les séquences T3 **n'ont pas exercé le déclencheur** (mise
  en veille de l'écran) : quatre minutes chacune, écran allumé. Elles établissent
  que la montée ne dégrade rien ; la discrimination vient du banc hors dépôt.
  `D-233` §3 le dit au lieu de laisser croire la preuve plus large.
- **N'atteint pas** — le rouge WebKit **du CI**, jamais expliqué par `D-049` ni
  par sa clôture. Sa règle survit, son mécanisme reste inconnu.
- **N'atteint pas** — aucun banc ne vérifie que T3 est joué avant une PR
  migration/scoring/clinique : la barrière est une règle, pas un garde. C'était
  déjà vrai avant `D-049`.

## 5. Fichiers modifiés

- `docs/DECISIONS.md` — `D-233` ; chapeau de clôture sur `D-049` ; mention de
  péremption sur l'amendement du 2026-09-17 ; ligne de statut de `D-049`.
- `.claude/rules/tests-validation.md` — la dérogation retirée, remplacée par
  quatre points : T3 entier exigé, le rouge WebKit du CI redomicilié, `retries`
  toujours interdit, et un blocage de la signature `D-049` désormais traité
  comme un fait neuf.
- `scripts/wn-diagnostic-e2e.mjs` — le **message** du classificateur seulement.
  L'en-tête historique n'est pas touché : il raconte pourquoi le script existe et
  reste exact.
- `changelog.d/2026-09-19-cloture-d049.md`, `docs/claude/SESSION_LOG.md`, ce
  handoff.
- **`CLAUDE.md` n'est pas modifié, et c'est le résultat attendu** : `D-049`
  promettait de le « rétablir à l'identique », mais la dérogation vivait dans le
  fichier de règles. Sa table de validation exige déjà T3 sans réserve.

## 6. Validations exécutées

- `node scripts/lib/decisions-numerotation.mjs` — OK, **231 décisions, sans trou**.
- `node --test scripts/wn-coherence-etat.test.mjs` — 29/29.
- `node --test scripts/wn-diagnostic-e2e.test.mjs` — **14/14**, après réécriture
  du message.
- `node scripts/wn-ancres-doctrine.mjs` — OK, 4 ancres.
- **T3 complet** (`npm run test:worktree`) sur ce lot — verdict dans la PR. Le
  jouer ici n'est pas un ornement : c'est le premier lot sous le régime qu'il
  rétablit.

`changelog-collate.mjs` n'a pas été lancé : destructeur sans argument, pas de
mode `--check`.

## 7. Problèmes ouverts

- **La réserve de WebKit 2359** : défaut signalé le 2026-09-17, signature
  `didReceiveInvalidMessage`, contournement amont « the retry passed » — or
  `retries` est interdit ici. Non observé sur les deux séquences. S'il paraît,
  c'est un fait neuf, pas le retour de `D-049` (`D-233` §5).
- **La séquence écran en veille n'a pas été jouée.** Elle confirmerait la clôture
  sur le déclencheur exact ; elle n'est pas un chaînon manquant, la
  discrimination étant faite sur le banc. Proposée au responsable, non demandée.
- **PR #1189 ouverte**, prête et verte, tenue en attente du mot du responsable.
- **Flake connu**, inchangé : `portail-parcours.spec.ts`, `socket hang up` /
  `ECONNRESET`, à ne pas confondre avec les signatures de `D-049`.

## 8. Prochaine action exacte

1. Revue lue aux trois emplacements ; merger avec
   `gh pr merge --squash --subject "…"` portant `D-233`, jamais sans.
2. Les deux points RGPD de `D-222` §2, réduits par les chiffres de production du
   2026-09-18 : la qualification juridique ne porte plus que sur **la voie
   d'exception** (lettre d'adressage sur signal d'alerte, seule route non gardée,
   qui part malgré un refus), et l'information des patients tient à **deux
   personnes** — accusés v8 sans v9 —, pas à un mécanisme.

## 9. Interdits encore actifs

- Aucune identité réelle au dépôt ; dossiers de test lus par identifiant.
- Aucun secret en dur ; production en lecture seule par conteneur détaché.
- Pas de migration Prisma ni de `schema.prisma` sans demande explicite.
- Aucun contenu clinique du corpus au dépôt tant que G6 est fermée.
- Une signature clinique ne se pose jamais par l'outil.
- **`retries` reste interdit à Playwright** — `D-049` close n'y change rien.
- **Un rouge WebKit du CI ne se relance jamais** (`D-155`, redomiciliée).
- Force-push, production et arbitrages restent à demander. Autorisation Git
  courante jusqu'au **2026-09-24**.
