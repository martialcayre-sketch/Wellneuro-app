# Handoff — 2026-08-17 — D-068 : catalogue biologie niveau 1 en migration

- **État** : `feat/catalogue-biologie-niveau-1-donnees` (rebasée sur main
  post-#698). T3 vert (20 contrats SQL — le nouveau compris — hors la
  signature WebKit `D-049`). `wn-reviewer` : deux NO-GO soldés, verdict
  conditionné aux trois gestes, tous faits avec la preuve exigée (19 → 20).
- **Contenu** : migration de données `20260817090000` (47 analytes, 15 panels,
  78 items, 2 plages conditionnelles à leur claim, colonne
  `validation_medicale_requise`, `µg/mL` aux TROIS vocabulaires d'unités),
  contrat de données `cb_catalogue_niveau_1_donnees.sql` (enregistré dans
  `ci.yml` — le harnais tire sa liste de là, pas du répertoire), `D-068`.

## Ce que la revue a imposé, à retenir

- **La forme d'une plage cite le claim qui la FONDE** : vitamine D [45, +∞)
  sur `0154-054` (« > 45 »), pas sur `0239-004` (« proche de 45 », cité
  concordant). Plafond manquant = écart de CORPUS nommé, jamais une
  affirmation silencieuse.
- **`pg_get_constraintdef` ne rend jamais `IN`** (normalisé `= ANY (ARRAY)`) —
  motif `ARRAY\[.*\]`, garde `IS NULL` conservée (une liste à un élément perd
  sa forme ARRAY et doit rougir).
- **Un contrat non enregistré dans `ci.yml` n'existe pas** : le harnais
  n'énumère pas `prisma/checks/`. La garde est asymétrique — présent au dépôt
  et absent du CI = silence.
- Compromis MI-9 écrit dans le contrat (comptes panels/items en totaux — un
  16ᵉ panel légitime rougira, c'est voulu). Dette : banc de lignée
  D-xxx ↔ migration (aurait attrapé BL-2).

## Après merge — OBLIGATOIRE

`release-db` (approbation praticien), puis vérification post-release : les
SEPT lectures MCP du bloc « risques » de la PR, plus la ligne
`NOTICE D-068 : N plage(s)` du log (attendu : 2). La lecture n° 4 (les deux
plages) est la seule qu'aucun contrat ne couvre (cas 0/0).

## Reste : PR-3, dernière marche

Règles d'indication (12 conditionnels — six en `ou` D-060 —, 1 optionnel,
2 non indiqués), banc d'inertie RV-1, garde de forme RV-2, adaptation fixture,
**signature biologie** (date ISO, claims, `shaPerimetre` littéral). Zones :
le §B de la proposition fait foi, pas la colonne du §F.2 (piège « warning
(5-21) » = étendue, pas bande).
