# Handoff — 2026-09-17 — LOT-06 : la mesure, protocole écrit et exécution due

Dernier lot de la campagne « ouverture du rayon Correspondance ». **Aucune décision**
— rien n'est tranché ici, et c'est le point.

## Branche et état Git

`correspondance-lot06-mesure`, branchée sur `origin/main` (`903e7e32`, tête après le
merge de [[D-222]]). **Aucun code.** Deux fichiers de documentation.

## Objectif, et pourquoi il existe

La campagne a livré cinq lots sans mesurer l'usage réel du rayon, et la seule mesure
qui existait est fausse par construction : un « 0 » relevé le 2026-08-14, **quatre
jours avant** l'ouverture du courrier de biologie.

## Décisions prises

Aucune. Le document est un **protocole**, pas un résultat. Trois choix de forme :

- **la ventilation sur `ancrage_sha256 is not null` est la colonne qui compte** —
  sans elle, la mesure additionne un geste humain et une génération machine ;
- **`ancrage_version` sépare les deux écrivains ancrés** depuis [[D-218]] : c'est la
  seule mesure qui dira si la lettre d'adressage a servi ;
- **trois gabarits de route**, pas deux : [[D-218]] en a ajouté un, et filtrer sur le
  premier seul sous-compte le rayon.

## Fichiers modifiés

- `docs/claude/campagnes/MESURE_RAYON_CORRESPONDANCE_2026-09-17.md` — **neuf**.
- `changelog.d/2026-09-17-mesure-rayon-correspondance.md` — **neuf**.

## Validations exécutées

T1 vert. **Pas de T2** : aucun code exécutable dans ce diff.

**Une requête a réellement tourné** — `one-off-8811`, app `wellneuro`, région
`osc-fr1`, lecture seule, agrégats seuls. **Sa sortie n'a pas été lue** : le
classifieur de sécurité de la session autonome a refusé `scalingo logs`, et **le
refus n'a pas été contourné**. C'est le même refus que la session du 2026-09-15 avait
rencontré sur la campagne « 5. Actions ».

## Problèmes ouverts

**La mesure elle-même.** Elle tient en une commande depuis une session attelée :

```bash
scalingo --region osc-fr1 --app wellneuro logs --filter one-off-8811
```

Les requêtes 2 et 3 n'ont pas été lancées. Si la fenêtre de logs est passée, relancer
la requête 1 telle quelle et lire le nouveau numéro — une lecture agrégée se rejoue,
rien n'est perdu.

**L'interprétation a une garde** : la lettre d'adressage ne peut rien mesurer tant
que `WN_ADRESSAGE_COURRIER` n'est pas posée en production. Mesurer avant de l'allumer
rendrait un zéro qui ne mesure rien.

**L'échéance est dure** : le journal d'accès est purgé à 12 mois glissants, la série
ne se reconstitue pas, et la revue RGPD du **2026-10-21** est le repère.

## Prochaine action

**La campagne est livrée** — six lots, six PR, cinq décisions. Restent trois gestes
qui n'appartiennent pas à une session : poser `WN_ADRESSAGE_COURRIER` en production,
lire la mesure, et trancher les réserves de [[D-222]] (les huit versions publiées du
registre TRUST, l'effet du refus, la traçabilité des formulations).

## Interdits encore actifs

Inchangés. Et celui que ce lot illustre : **un refus du classifieur de sécurité ne se
contourne pas**. Il se consigne, avec la commande exacte qui manque.
