# Handoff — 2026-08-08 00:30 — Clôture de la campagne « Dettes Wellneuro 5.0 »

Campagne `2026-08-05-cloture-des-dettes-wellneuro-5-0` **close**, ses huit lots
livrés. Aucune campagne active ; `.wn/state.json` passe à `idle`.

## Le verdict

**Wellneuro 5.0 n'est pas déclarable en bloc.**

| Verdict | Dettes |
|---|---|
| Fermées | 1 (consommation), 3 (moteurs de scoring), 7 (chemin d'écriture en base) |
| Arbitrée et reportée | 8 (HDS) — revue au **2026-10-21** |
| **Ouvertes** | 2 (validation psychométrique), 4 (double source des packs), 5 (deux parcours patients), 6 (état réel) |

Preuves dette par dette :
`docs/claude/campagnes/2026-08-05-cloture-des-dettes-wellneuro-5-0/DECLARATION_5_0.md`.

## Ce qu'il faut savoir avant de reprendre

**Une section `## Résultats` est une déclaration de son auteur, pas une preuve.**
Quatre verdicts sur huit diffèrent de ce que le lot concerné écrivait de
lui-même, et aucun de ces écarts n'était visible sans exécuter le script, lire le
code ou compter le registre :

- **LOT-01** déclare comparer trois dimensions sur six ; le script n'en compare
  plus qu'**une**. Deux comparaisons ont été retirées le 2026-08-07 sans que la
  prose suive.
- **LOT-04** coche « une date de retrait existe » ; il n'y en a aucune — la date
  au code est celle de la décision.
- **LOT-02** annonce le repli legacy journalisé ; le cas qui compte l'était déjà
  avant, et ce qui a été ajouté couvre deux cas à zéro occurrence.
- **LOT-03** est juste dans le code, mais deux commentaires de
  `web/src/lib/clinical/` déclarent toujours ses moteurs « ouverts ».

**Le « zéro écart » de `wn-etat-reel` n'engage presque rien.** Il observe six
dimensions et n'en compare qu'une (`validation.last_checked_at`). Le lot courant
n'est comparé par rien : `.wn/state.json` portait `LOT-06` quand `CAMPAGNE.md`
disait LOT-07, sans que l'outil le voie. Ne pas se prévaloir de ce zéro.

## La prochaine campagne, dans cet ordre

1. **Dette 6 d'abord** — sans un outil d'état qui compare vraiment, tout jugement
   sur les autres dettes est invérifiable.
2. **Dette 5** — poser une date de retrait réelle ; retirer le lien interne
   `href={/patient/...}` survivant (`page.tsx:192`). Petite et bornée : le
   *risque* est fermé, seul le retrait est sans date.
3. **Dette 4** — resynchroniser le pack en dérive
   (`PACK_-bG21yeIvVYRhrdlYuWIMnFz`, `Q_SOM_09` absent du registre), ou fermer le
   repli en connaissance de cause.
4. **Dette 2** — décider si la validation psychométrique entre au programme, ou
   écrire qu'elle n'y entre pas.

Plus deux corrections mineures : les commentaires faux de `orientationEngine.ts`
et `orientationRulesV1.ts`, et la date d'arbitrage HDS divergente d'un jour
(2026-07-22 dans `CAMPAGNE.md`, 2026-07-21 dans le dossier RGPD).

**Aucune de ces quatre dettes n'a été posée en lot de la campagne close** — la
garde `closed_campaign_with_open_lots` l'interdit, et elle a raison.

## PR

La #435 est mergée, la **#372 fermée sans merger** au LOT-07 (périmètre absorbé
par #566/#567), la #618 mergée le 2026-08-08. Aucune PR ouverte au moment de la
clôture — instantané daté, pas une propriété : la #618 était encore un brouillon
`BLOCKED` quelques heures plus tôt.

## Validation

T3 complet vert le 2026-08-08 : séquence CI entière en 2 min 08, 122 tests E2E
passés, 2 ignorés, Chromium + WebKit, PostgreSQL éphémère.
