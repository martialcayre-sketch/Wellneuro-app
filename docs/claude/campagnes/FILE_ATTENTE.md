# File d'attente des campagnes — hiérarchie du 2026-08-21 (axe 6.0)

> **Réconciliée le 2026-08-27.** Le fichier promet de se mettre à jour « à
> chaque ouverture ou réarbitrage » ; il ne l'avait pas été depuis le
> 2026-08-25. Trois écarts constatés et corrigés ci-dessous : une campagne
> **ouverte, menée et livrée sans jamais entrer dans la table** (l'objectif à
> trois voix) ; une **collision de nom** qu'elle a introduite sur « 6.0-B » ; et
> des pointeurs « prochaine en file » devenus faux. La réconciliation ne
> réarbitre rien — elle rend la table fidèle à ce qui s'est passé.

## `6.0-B` DÉSIGNE DEUX CAMPAGNES SANS RAPPORT — toujours qualifier

L'axe 6.0 attribuait les lettres à l'ouverture : **A** = le dossier à deux voix,
**B** = charge et capacité, **C** = le récit, **D** = le jumeau. La campagne
ouverte le 2026-08-23 s'est intitulée **« Alliance 6.0-B — l'objectif à trois
voix »**, reprenant une lettre déjà attribuée.

Les deux existent, aucune ne se renomme : « 6.0-B » figure dans `D-110`,
`D-111`, `D-112` et dans les messages de commit, et l'historique Git ne s'efface
pas. **Un « 6.0-B » nu est donc ambigu** — c'est exactement le régime déjà en
vigueur pour le préfixe `R` (CLAUDE.md : « toujours qualifier la série »).

- **6.0-B *l'objectif à trois voix*** — `2026-08-23-alliance-objectif-trois-voix/`, livrée.
- **6.0-B *charge et capacité*** — `2026-08-21-charge-et-capacite/`, rang 5, non ouverte.

## D'où vient cette file

Réarbitrée en session le 2026-08-21, sur l'architecture de campagnes issue de
l'audit « Wellneuro face au notebook 00 » (audit → contre-audit adversarial →
vision 6.0 « alliance thérapeutique » → architecture, §8 de l'artifact).
Constat fondateur : le portefeuille était entièrement *moteur et données* ;
l'axe alliance — la réponse au trou Éducation thérapeutique de l'audit —
n'avait aucun véhicule. Cinq dossiers 6.0 entrent en file en `--init-only` :
le cadrage complet (CAMPAGNE.md, lots) s'écrit au moment d'ouvrir chaque
campagne, avec un état réel frais — jamais d'avance. Ce fichier porte l'ordre
et sa raison ; il se met à jour à chaque ouverture ou réarbitrage.
Hiérarchie précédente : celle du 2026-08-18 (l'historique Git la conserve).

## L'ordre, et pourquoi

> **Créneau primaire au 2026-09-16 : « Le rayon Patients — le dossier rendu au
> praticien »** (`2026-09-16-rayon-patients/`). Elle n'entre pas dans la
> hiérarchie 6.0 ci-dessous — elle vient d'une **demande du responsable**,
> formulée en session le 2026-09-16, captures à l'appui. L'entrée est posée ici
> **à l'ouverture**, et non à la clôture : c'est l'écart que la réconciliation
> du 2026-08-27 a reproché deux fois de suite. « 5. Actions — le protocole
> assisté » est close le même jour, neuf lots, sur un usage mesuré à zéro.
>
> *Créneau primaire précédent, au 2026-09-14 : « 5. Actions — le protocole
> assisté » (`2026-09-14-protocole-assiste/`) — venue d'une **mesure de
> production**, pas d'un arbitrage de portefeuille : 7 épisodes T0 confirmés et
> zéro version de protocole C1, le 2026-09-13. Elle a absorbé l'entrée
> « Producteur d'intentions `conditionnelle_biologie` » plus bas. Alliance 6.0-B
> l'objectif à trois voix est close le même jour, sept lots sur sept, sur un
> bilan qui refuse de signer le classement.*

| Rang | Campagne | Dossier | Raison du rang |
|---|---|---|---|
| 0 | Biologie consolidée | `2026-08-18-biologie-consolidee/` | **TERMINÉE (2026-08-22)** — les trois lots livrés (LOT-01 PR #725, LOT-03 PR #731, LOT-02 PR #726). Le créneau primaire s'ouvre ; prochaine en file : le Socle (rang 1) — l'ouverture reste un geste du responsable. |
| — | Échéance HDS — G-TRUST-04 | `2026-08-18-echeance-hds-g-trust-04/` | **PARALLÈLE, et la seule fenêtre qui SE FERME.** Bloquée sur l'externe pour l'annexe (Scalingo muette) ; revue du 2026-10-21 inchangée ; ne consomme pas le créneau primaire. **Mais son LOT-02 porte une échéance au 2026-09-01** (`D-080`) : décommissionnement Vercel/Supabase **avec preuve d'effacement écrite au registre RGPD**, et constat de levée de la réserve annexe HDS (`D-089`). Le jour où la base de rollback disparaît, tout code ou outil pointant encore vers Supabase devient un piège muet — le MCP `execute_sql` lit précisément cette base gelée. **Travail de dépôt identifié le 2026-08-26** : 5 scripts npm `supabase:*` morts, `vercel.json`, `vercel-build.sh`, `clone_env_vars.py`, la documentation qui décrit encore le filet, et le seul point sensible `withSupabaseSslMode` (chaîne de connexion — à vérifier contre la base réelle avant d'y toucher). Les migrations qui nomment Supabase sont historiques et **ne se touchent pas**. |
| 1 | Socle de restitution sûre | `2026-08-21-socle-restitution-sure/` | **TERMINÉE (2026-08-22)** — ouverte et livrée le même jour, trois lots : couverture des chemins sortants prouvée par bancs de débranchement + re-vérification du bilan (LOT-01, PR #736), clinique au niveau « demande » + D-083 (LOT-02, PR #739), registre de gabarits `DC-26` (LOT-03). **Le gate des campagnes 6.0 est posé.** Le créneau primaire s'ouvre ; prochaine en file : 6.0-A (rang 2) — l'ouverture reste un geste du responsable. Restent au responsable : validations `valideLe` des gabarits, arbitrage des régimes de garde, candidats de couverture du hook. |
| 2 | Alliance 6.0-A — le dossier à deux voix | `2026-08-21-alliance-dossier-deux-voix/` | **TERMINÉE (2026-08-22)** — six lots mergés (#748, #750, #754, #755, #757, #760). Le gate `D-092` est constaté en production sur sa structure : cinq tables et écrivain unique prouvés, zéro ratification au moment de la clôture. Cela n'active ni les deux surfaces encore fermées (`WN_CE_QUI_COMPTE`, `WN_DOSSIER_DEUX_VOIX`) ni le chemin élargi protocole→produits, qui restent des gestes du responsable. Le créneau primaire s'ouvre ; prochaine en file : Doctrine exécutable (rang 3) — son ouverture reste un geste du responsable. |
| 3 | Doctrine exécutable | `2026-08-18-doctrine-executable/` | **TERMINÉE (2026-08-25)** — neuf lots exécutés (LOT-01, 03, 04, 05, 06, 07, 09, 10, 11), plus un LOT-12 **non prévu au cadrage** né d'une contre-revue adverse, et le LOT-08 de clôture. **Six règles franchissent leurs trois preuves** — `DC-09`, `DC-19`, `DC-22`, `DC-23`, `DC-54`, `DC-55` — chacune vérifiée par le lot de clôture, banc nommé, jamais sur la foi d'un lot déclaré terminé. **Ce qui n'est PAS fermé est écrit** : `DC-20` (nature en prose, pas dans la donnée → rang 4), `DC-26` (compilateur inexistant), `DC-42` (**signature reportée au 2026-08-30**), `DC-43` (mécanisme complet, **sans sujet** — `neCouvrePas` null sur les 95), `DC-58` (instruite, sans méthode fondée), les **quatre non armées** revérifiées structurellement, et les **onze statuts orphelins** → campagne dédiée (`à cadrer`). `DC-50`/`DC-51` **renvoyées** à la chaîne alimentaire — un renvoi est un routage, pas une fermeture. **La contre-revue adverse a payé** (`D-108`) : lancée AVANT la clôture, elle a réfuté **sept** des treize affirmations qui allaient être gravées, dont un texte servi au patient depuis cinq semaines. ~~Le créneau primaire s'ouvre ; prochaine en file : Curation signée (rang 4).~~ **Pointeur périmé, corrigé le 2026-08-27** : ce n'est pas Curation signée qui a suivi, mais **6.0-B *l'objectif à trois voix*** (rang 3 bis), ouverte hors table le 2026-08-23. Restent au responsable : la signature `SAFETY_EI_METADATA` (revue 2026-08-30), le cadrage des **deux** campagnes routées (curation des exclusions, dix orphelines), l'arbitrage de la gate sur un état INCONNU, et le seuil de significativité du momentum. |
| 3 bis | **Alliance 6.0-B — l'objectif à trois voix** | `2026-08-23-alliance-objectif-trois-voix/` | **LIVRÉE, NON CLOSE (2026-08-26)** — ouverte le 2026-08-23 **hors de cette table**, qui n'en a jamais porté trace jusqu'à la réconciliation du 2026-08-27 : c'est l'écart que celle-ci corrige, et il explique la collision de nom ci-dessus. Six lots livrés (PR #748→#802 pour la série complète ; LOT-05 en trois PR — #799 migration, #800 code, #801 correctifs de revue). **Le LOT-06 a rendu un bilan qui contredit l'attente du portefeuille** (`D-112`) : les **neuf tables de la campagne portent zéro ligne** en production, et **zéro épisode `T0` n'est confirmé** sur 21 dossiers — l'appareil est complet et n'a jamais servi. `D-093` n'est donc pas levé : sa condition (a) bute sur l'absence d'objectif, sa condition (b) n'est pas « non atteinte » mais **non productible**. Le dossier de signature du classement **n'est pas préparé, et ce refus est motivé** (`DC-19` : rien à certifier, le rédiger supposerait un comportement). **Restent avant clôture** : passe Codex du LOT-05 (P0) et **contre-revue adverse de campagne** — avant, jamais après. |
| 4 | Curation signée | `2026-08-18-curation-signee/` | **En parallèle continu, pas en séquence** : cadence praticien (claim par claim). NABM, liens biomarqueur↔besoin, question D-062. Chaque claim curé rapproche l'activation du chemin protocole→produits — donc renforce le rang 2. **Périmètre entrant le 2026-08-23** (`D-096`) : la migration des trois axes doctrinaux du claim — catégorie `A-E` (`DC-07`), niveau d'exécution (`DC-13`), nature du seuil (`DC-20`) — transférée depuis « Doctrine exécutable », faute de consommateur là-bas. Elle porte désormais **la structure ET le contenu** de ces axes, plus la matrice claim par claim. Source à reprendre au cadrage : `sources/2026-08-23-transfert-migration-axes-claim.md`. **Toujours à l'arrêt** : appariement NABM et liens biomarqueur↔besoin à 0 ligne. **CADRÉE LE 2026-09-16** — `CADRAGE_CURATION_SIGNEE_2026-09-16.md`. L'audit du corpus du même jour a mesuré que **cinq axes cliniques entiers n'ont aucun claim validé** (mémoire et troubles cognitifs, TDAH/scolarité, autisme, douleurs chroniques, intestin-cerveau, cas complexes) : 755 claims en attente à l'instantané du 2026-08-03, **chiffre à recompter avant toute cadence**. La mécanique de signature est COMPLÈTE et en service, voie rapide comprise (routes `claims/lot/tirage` et `claims/lot/decision`) : le cadrage n'ouvre aucun chantier d'ingénierie, il ouvre une cadence. |
| 4 bis | **Biologie exploitée** | `2026-09-04-biologie-exploitee/` | **Cadrée le 2026-09-04**, rang posé le même jour. Quatre lots courts, aucune règle clinique, aucune migration : relecture du document patient consigné + garde anti-double-consignation, geste de correction d'une saisie, E2E des étages de septembre, plage sourcée à côté de la mesure (conditionné au tranchement `DC-19`/`DC-20`). **Raison du rang — elle passe devant le rang 5, et ce n'est pas une préférence de sujet** : (1) son LOT-01 **répare une surface vivante** — `WN_CB_PROPOSITION` est allumé en production depuis le 2026-08-18, le geste « document patient » sert depuis le 2026-09-03, et ce qu'il consigne n'est **relisible nulle part**, avec une double consignation possible par deux onglets (défaut nommé le 2026-08-20, sans lot d'accueil jusqu'ici) ; (2) le **fait nouveau `D-112`** qui pèse sur le rang 5 pèse dans l'autre sens ici — le goulot est le temps praticien, or cette campagne n'ajoute **aucune surface**, elle rend utilisable du déjà-servi ; (3) elle est **petite et sans dette d'attente** : LOT-01 et LOT-03 n'ont aucune dépendance, les deux arbitrages en suspens (régime de correction, question `DC-19`/`DC-20`) ne bloquent que LOT-02 et LOT-04. **Ordre interne qui en découle** : LOT-01 et LOT-03 d'abord (surface vivante) ; LOT-02 et LOT-04 suivent le drapeau `WN_CB_RESULTS_ENABLED`, dont la levée reste un geste du responsable. **Ne préempte pas le rang 3 bis** : la clôture de 6.0-B *objectif* (passe Codex du LOT-05, contre-revue adverse de campagne) est une clôture, pas une ouverture. |
| 4 ter | **5. Actions — le protocole assisté** | `2026-09-14-protocole-assiste/` | **CLOSE LE 2026-09-16.** Ouverte hors table le 2026-09-14 — l'écart que la réconciliation du 2026-08-27 avait précisément reproché à 6.0-B *objectif* ; l'entrée est posée ici à la clôture pour ne pas le répéter une troisième fois. **Neuf lots** (les huit du cadrage + un LOT-08 né de la contre-revue), **neuf décisions** (`D-188`→`D-193`, `D-196`, `D-198`, `D-200`), zéro migration, zéro drapeau neuf. **Contre-revue adverse faite AVANT la clôture** (patron `D-108`) : 26 affirmations soumises, **6 réfutées**, 4 défauts réels corrigés — dont le refus de registre anxiogène **invisible sur le chemin de révision**, récidive exacte du défaut du booklet. **USAGE MESURÉ AU CONTENEUR LE JOUR DE LA CLÔTURE : zéro version de protocole C1 en production.** L'entonnoir franchit la phase Décision (T0 de 7 à 8, sélections de priorité de 1 à 3) et s'arrête à la phase Actions. Le chiffre mesure un jour de fonctionnement — relecture posée en entrée « à cadrer » ci-dessous. **Le créneau primaire s'ouvre ; son attribution reste un geste du responsable**, et `D-112` pèse maintenant sur trois campagnes de suite. |
| 4 quater | **Le rayon Patients — le dossier rendu au praticien** | `2026-09-16-rayon-patients/` | **OUVERTE LE 2026-09-16**, au créneau primaire, sur **demande du responsable** — pas sur un arbitrage de portefeuille ni sur une mesure. L'entrée est posée **à l'ouverture**, pour ne pas répéter l'écart reproché à 6.0-B *objectif* puis à *protocole assisté*. **Huit lots, une PR chacun, base `main`.** Le chantier répond à trois manques constatés dans le code le jour même : le dossier ne se modifie pas (`PATCH /api/praticien/patients` n'accepte que `telephone` et `actif`) ; adresse, NIR et médecin traitant n'existent nulle part ; et **la fiche signalétique et l'anamnèse, écrites par le patient à l'ouverture de son espace, ne sont lues par AUCUNE surface praticien** — seul `lib/synthese/generation.ts` y touche, pour fabriquer un texte. **L'ordre place le LOT-02 (relecture des renseignements) AVANT la migration** : il ne dépend d'aucune colonne neuve et part sans attendre la porte `release-db`. **Une seule migration** (`LOT-03`), additive, quatre colonnes nullables sur `patients`, seule dans sa PR (`D-087`) — et elle ouvre une **fenêtre d'indisponibilité** entre le merge et l'approbation, Prisma sélectionnant toutes les colonnes scalaires du modèle. **Restent au responsable** : la qualification du NIR au titre de l'article 9 — le dossier RGPD refuse explicitement de la poser dans le code — et le créneau de merge du LOT-03. Arbitrages rendus le 2026-09-16 : rail et URL, destination des assignations et des packs, périmètre du rayon, forme d'affichage des renseignements, document patient v8 **avec accusé exigé**, NIR en clair. |
| 5 | 6.0-B ***charge et capacité*** (à ne pas confondre avec 6.0-B *l'objectif à trois voix*, rang 3 bis) | `2026-08-21-charge-et-capacite/` | **FAIT NOUVEAU DU 2026-08-26, à peser avant d'ouvrir** : le bilan `D-112` a constaté **zéro usage** de tout ce que 6.0-A et 6.0-B *objectif* ont construit. Ce rang ajoute une **surface de plus** ; le goulot constaté n'est pas l'ingénierie, c'est le temps praticien. Le rang n'est pas réarbitré ici — la réconciliation ne décide pas —, mais l'ouvrir sans avoir lu `BILAN.md` serait ouvrir à l'aveugle. Budget d'effort, « simplifier mon protocole » (`ProtocolDraft` chaîné), mode « semaine compliquée », check-in v3 additif. Dépend de 6.0-A ; arbitrage A1 intangible (pilotage, jamais score). |
| 6 | 6.0-C — Le récit du parcours | `2026-08-21-recit-du-parcours/` | Timeline racontée (projection), petites victoires (jamais causales, `DC-27`), « pourquoi maintenant ? » + double lecture (`DC-34/35`), hypothèses partagées (`DC-31/32`), messages du registre signé. Dépend du Socle et de 6.0-A. |
| 7 | 6.0-D — Le jumeau de compréhension | `2026-08-21-jumeau-de-comprehension/` | La signature conceptuelle : représentations patient/praticien côte à côte, versionnées, écarts visibles, « prochain choix ensemble » aux jalons. Dépend de 6.0-A et 6.0-C. |
| 8 | Nutrition référentielle (R1→R3) | `2026-08-18-nutrition-referentielle/` | Recule d'un cran par réarbitrage 6.0 : ses fiches conseils consomment le registre de messages du Socle — l'ordre naturel la place après lui. Premier lot inchangé (recouvrement rayon C4). |
| ~~à cadrer~~ **LIVRÉ** | ~~**Le verbe de départage de deux têtes d'objectif**~~ — **livré le 2026-09-10** : route `POST /api/praticien/objectifs/fin` (motif `remplace`), lecture par `tetesDeChaine`/`tetesActives`, geste au cockpit, garde de frontière. La discordance ne compte plus que les têtes ACTIVES. | **Historique de l'entrée :** *(pas de dossier — arbitré le 2026-09-10)* | **CORRIGÉ LE 2026-09-10 — ce chantier n'a jamais été « 15-30 lignes sans migration ».** Une tête est une ligne que personne ne supplante, et `supersedes_objectif_id` est à PARENT UNIQUE : ajouter une ligne retire une tête et en ajoute une, ajouter une ligne sans parent en ajoute une. **Aucune suite d'ajouts ne peut faire DÉCROÎTRE le nombre de têtes** — ce n'est pas une lacune d'implémentation, c'est une propriété du modèle. Le départage exige donc une table que le calcul des têtes consulte, et c'est exactement le motif `remplace` de [[D-161]] : **la table de fin n'arrive pas après le départage, elle EST le départage.** Migration écrite le 2026-09-10. **Le seul vrai blocage du parcours à deux voix.** Deux têtes de chaîne ferment les TROIS gestes du patient en 409 `objectif_discordant` (`api/portail/dossier/route.ts:619`, `:618-624`) ; le cockpit n'affiche qu'un avertissement et n'offre aucun geste (`ObjectifNegociePanel.tsx:924-931`). **Aucune sortie applicative n'existe** : réviser chacune des deux en laisse deux, il n'y a ni fusion, ni abandon, ni suppression hors effacement du dossier entier — **le nombre de têtes est monotone croissant**. Et la seconde tête ne demande **aucune course** pour naître : reprendre une proposition alors qu'un objectif courant existe la produit mécaniquement, la route refusant le cumul `supersedesObjectifId` + `sourcePropositionId` en 400 et l'écran offrant « Reprendre cette phrase » sans regarder `objectifs.length` (`api/praticien/objectifs/route.ts:755`). Rien en base ne garantit l'unicité : ni contrainte, ni index partiel — un lire-puis-409 hors transaction que le code déclare lui-même non étanche (`migrations/20260822153000_alliance_dossier_deux_voix_v1/migration.sql:99`). **Ce qu'il conditionne** : le bouton « Reprendre cette phrase » de `D-160`, et la table de fin de `D-161` (poser un objet de fin sur une chaîne qui peut se dédoubler ajouterait un objet à un état déjà cassé). Correctif R1 nommé à la contre-revue, 15-30 lignes. **Aggravant consigné** : les deux chemins qui ajoutent une racine notifient le patient en lui promettant des gestes que le portail vient de fermer. |
| ~~à cadrer~~ **LIVRÉES** | ~~**Les trois réparations qui ferment le parcours de l'objectif partagé**~~ — **les trois livrées le 2026-09-10** (#977 vidage du formulaire, #979 relance, #984 rail de la phase 3) | *(pas de dossier — arbitré le 2026-09-10)* | Trois gestes distincts, dans cet ordre, après le départage. **(1) ~~Le vidage du formulaire à chaque bascule de mode~~ — LIVRÉ le 2026-09-10** (`viderDeclarations`, appelé aux six bascules ; trois bancs qui tombent sans lui) : le panneau n'est jamais démonté et aucune des trois annulations ne le vide, si bien qu'une date d'accord saisie puis abandonnée **repart avec la version choisie ensuite** — ce n'est pas une perte, c'est une **date fausse affichée au patient** (`ObjectifNegociePanel.tsx:383`, `:528`, `:1084-1089`, `:1251`). À faire en premier : un patient lit aujourd'hui un « Convenu le » qui peut être inventé. **(2) ~~L'action « relancer »~~ — LIVRÉE le 2026-09-10** (`POST /api/praticien/objectifs/relance` ; cadence de trois jours empruntée aux relances d'agenda et opposable côté serveur ; bouton au cockpit ; **ne crée aucune ligne**) : faire partir le courrier d'un objectif **déjà écrit**, sans toucher à la chaîne. `notifierObjectifPropose` n'est appelée que sur les deux chemins de `create` (`api/praticien/objectifs/route.ts:877`, `:912`) ; aucun renvoi n'existe, et l'unique objectif de production, sur `PAT017`, est **muet par construction**. La relance emprunte la doctrine de cadence déjà opposable au dépôt (`JOURS_ENTRE_RELANCES`, `MAX_TENTATIVES_FENETRE`), que la notification d'objectif n'emprunte pas. **(3) ~~Le rail cesse de mentir~~ — LIVRÉ le 2026-09-10** (route `objectifs/etat-phase` dédiée et journalisée ; « renseignée » exige un objectif actif ET une synthèse publiée ; une lecture en échec rend « indéterminée ») : le statut de la phase 3 ne lit que les couvertures des douze besoins — ni objectif, ni ratification, ni synthèse — et affiche « Compréhension — renseignée » sur un dossier sans le moindre objectif (`FichePatientPanel.tsx:747-749`). `D-161` §10 en fait une condition du passage à la prise de décision. |
| ~~à cadrer~~ **LIVRÉ** | ~~**Ce que le bilan du classement laisse à faire**~~ — **les cinq gestes livrés le 2026-09-10** (#978 textes figés et décompte, #980 égalité de plainte, #985 signé / non signé, #981 textes de sécurité) | *(pas de dossier — arbitré le 2026-09-10, sur `BILAN_CLASSEMENT_DESCRIPTIF_2026-09-09.md`)* | **(1) ~~Figer par un banc les quatre `LIMITATION_*` lus par le praticien~~ — LIVRÉ le 2026-09-10** (`LIMITATION_CLASSEMENT` épinglé verbatim, `LIMITATION_ETAT_INCONNU` passé de l'incipit au texte entier ; la mutation du premier fait rougir le banc), `LIMITATION_CLASSEMENT` en premier : son texte n'a **qu'une occurrence au dépôt, sa définition** (`chaineC1.ts:236-237`) — aucun banc, une réécriture passerait au vert —, alors que `LIMITATION_PROPOSITION` est bien épinglée dans `chaineC1.test.ts`. C'est la phrase qui porte toute la contrepartie du classement non signé. **(2) ~~Dire l'égalité de plainte dominante~~ — LIVRÉ le 2026-09-10** (champ `exAequo` sur `PlainteDominante`, phrase à l'écran ; l'ordre du classement n'est pas touché) : à intensité égale, l'ordre de publication du catalogue départage en silence (`chaineC1.ts:204-211`, `questionnaires/mode-de-vie.ts:25-31`). Garder ce départage technique pour la stabilité de l'affichage, et **signaler l'égalité** au praticien — `DC-30`, une discordance se signale, elle ne se résout pas. Ce n'est PAS le départage clinique que le code dit non rendu depuis `D-054` : c'est le refus de trancher à la place du patient. **(3) ~~Séparer signé et non signé à l'écran~~ — LIVRÉ le 2026-09-10** (deux groupes aux intitulés de provenance, sans badge ; le champ `limitationsRegleSignee` nomme le sous-ensemble relu, et tout texte non rattaché tombe du côté non signé) : les limitations de la règle relue, les quatre textes du moteur et le motif de gate arrivent dans un seul tableau dédoublonné (`chaineC1.ts:513-534`, `DecisionSummaryCard.tsx:58-62`). Séparer **sans badger** le signé — un badge sur-promettrait une certification que le SHA ne couvre pas. **(4) ~~Faire parvenir au praticien les trois textes `LIMITATION_*` de sécurité~~ — LIVRÉ le 2026-09-10** (section « Ce qui suspend la décision » ; le CONSTAT lui-même n'atteignait aucun humain non plus, et c'est pourquoi ses textes n'y arrivaient pas), arbitré le 2026-09-10 : ils traversent la réponse HTTP sans qu'aucun composant les rende (`decisionCard.ts:172`, `ClinicalRuntimeSection.tsx:1431-1432`). **(5) ~~Corriger le décompte du bloc « À LIRE AVANT DE RE-SIGNER »~~ — LIVRÉ le 2026-09-10** — « chacune des deux règles » quand la table en porte quatre depuis le 2026-08-28 (`priorityRulesV1.ts:496`) ; à faire dans le geste de la prochaine signature. **Hors périmètre de cette entrée** : seul le candidat courant voit ses limitations affichées, les rangs 2..n portant les leurs dans le JSON sans être rendus. |
| ~~à cadrer~~ **LIVRÉE** | ~~**La table de fin d'objectif**~~ — **livrée et APPLIQUÉE EN PRODUCTION le 2026-09-10** : migration `20260910120000_alliance_fin_objectif_v1`, run `release-db` 34414718699, constatée par conteneur (table présente, 12 colonnes, 10 CHECK, aucune unicité, RLS deny-all, 0 ligne). L'ordonnancement annoncé ci-dessous s'est révélé inverse — la table N'ARRIVE PAS après le départage, elle EST le départage. | *(pas de dossier — `D-161` pose la forme, ordonnancement arbitré le 2026-09-10)* | `D-161` a posé la forme et il n'y a plus rien à arbitrer : une **ligne append-only référençant la RACINE de la chaîne** — celle dont `supersedesObjectifId` est nul, qui ne bouge jamais, de sorte qu'une fin survit à toute révision et ne peut pas devenir orpheline — et **trois motifs**, atteint / abandonné avec motif / remplacé avec pointeur. « Atteint » se déclare à deux voix, la seconde valant par un geste au portail ou par une parole en consultation attestée ; le renoncement reste unilatéral et motivé. La clôture du suivi rend la chaîne **inactive** sans l'achever, et ne s'écrit pas. **ORDRE CORRIGÉ LE 2026-09-10, ET IL EST INVERSE DE CE QUI ÉTAIT ÉCRIT.** La table ne vient pas après le départage : elle le rend possible, le motif `remplace` étant le seul geste qui retire une tête. Migration `20260910120000_alliance_fin_objectif_v1` écrite le 2026-09-10, **seule dans sa PR** ([[D-087]]) — le code consommateur n'arrive qu'après application `release-db` approuvée puis CONSTATÉE par conteneur. **Le changement de lecture qui suit** : `objectifsCourants` reçoit les lignes de fin en second paramètre **OBLIGATOIRE**, non facultatif — le patron `etatRatification` tolère un défaut vide parce que son argument AJOUTE un état et ne peut qu'être incomplet ; une ligne de fin RETIRE une tête, et un défaut vide y fabriquerait un faux (chaînes closes servies comme actives, `ratifiable` vrai sur un objectif clos, écriture permise dessus). Casser les cinq appelants à la compilation est le comportement sûr. Et l'affichage se sépare de la discordance : on sert toutes les têtes, closes comprises et MARQUÉES ; on ne compte que les actives pour le 409. **Ce que la migration emporte** : `nonTraiteMotif` / `nonTraiteDepuisLe` promus de la version à la chaîne, et `negocieLe` qui cesse d'être saisie sur la version — la date d'accord se lit depuis le fait qui la porte (`D-161` §11). |
| à cadrer | Ré-alimentation du moteur par le mesuré | *(pas de dossier — frontière D-122, consignée le 2026-09-04)* | **Le différenciant produit** (croisement questionnaires × biologie de la roadmap) et la plus grosse suite du rayon : faire parler un résultat réel au moteur d'orientation/momentum est une **règle clinique neuve** — D-122 la ferme explicitement. Chemin : proposition de cadrage (patron du 2026-07-25) → décision D-xxx + claims validés (DC-19/20, DC-30 : une discordance se signale, ne se résout pas) → campagne propre. Ne s'ouvre pas sans le mesuré en production (levée du drapeau) ni sans matière (curation). |
| à cadrer | Import laboratoire (`import_labo`) | *(pas de dossier — consigné le 2026-09-04)* | Seconde origine des résultats (D-122 §2) : l'enum l'attend en base, rien ne le produit. Cadrage propre requis : formats, parseur, authentification de la source, doublons, **exactitude décimale de bout en bout** (la valeur transite en `number` JSON aujourd'hui — dette nommée à la contre-revue), et volet RGPD (nouvelle provenance de données de santé, sous-traitant éventuel). |
| ~~à cadrer~~ **ABSORBÉE** | ~~**Producteur d'intentions `conditionnelle_biologie`** (+ contrat V4 CB-07 transféré)~~ — **cadrée le 2026-09-14, devient le LOT-05 de « 5. Actions — le protocole assisté »** | `2026-09-14-protocole-assiste/lots/LOT-05-suspendre.md` | **Les deux chemins sont départagés, et il fallait le faire explicitement : aucun arbitrage consigné ne les tranchait.** Le dépôt se contredisait — `ProtocolMiniBuilder.tsx:255-260` interdit la saisie manuelle au nom de `D-056`, l'en-tête de `biologie-arbitrage-revision.spec.ts:17-24` affirme l'inverse, et `D-130` disait « le geste d'écran reste dû ». **Retenu : le geste au praticien, et SEULEMENT pour suspendre** — il pose `conditionnelle_biologie` avec sa cible, jamais `active`. La crainte de `D-056` était qu'une intention naisse *active* sans règle derrière ; ce geste ne fait que retenir. Une décision amende `D-056` et réécrit le commentaire. **Écarté : brancher `D-056`**, avec son motif — `deciderIntentionAvantBiologie` est écrit et testé mais refuse tout (`clinical_rules` 0 ligne, catalogue d'alertes non publié, lien règle↔claim manquant, `D-133`) ; il attend une curation qui n'a pas commencé. `BiologyCatalogRef` se réexamine dans ce lot, sur le patron de re-dérivation serveur d'`assertFoodCompassActionRef` et non sur celui, seulement structurel, de `SupplementCatalogRef`. **Défaut découvert au cadrage** : `reviserApresArbitrages` appelle `saveVersion` sans `version` — sur une version active V4 la route rend 409 `version_contrat_incompatible`. La boucle n'est pas seulement sans amorce, elle est sans issue ; corrigé dans le même lot. |
| à cadrer | Curation des exclusions d'intervention (`neCouvrePas`) | *(pas de dossier — routée le 2026-08-24, `D-107`)* | **Arbitrage praticien du 2026-08-24, qui REVIENT sur le constat de `D-101`.** Le LOT-05 avait ABANDONNÉ cette curation sur mesure — son registre n'a aucun consommateur d'exécution. Le responsable rouvre : les **95 interventions** portent `neCouvrePas` **null sur les 95**, et tant qu'elles le restent, `gatePopulationV1` ne mord sur **aucun** dossier et `DC-43` ne peut pas franchir son gate faute de **sujet**, non faute de mécanisme. Le mécanisme, lui, est **complet et relu** (`D-101`) : il ne manque que la donnée. **Ce que cela débloque** : `DC-43` obtient un porteur nommé au lieu d'être reconduite « écrite, non armée ». **Ce que cela coûte** : une cadence praticien, intervention par intervention, comme « Curation signée ». **Garde-fou non négociable** (`D-101`, `DC-35`) : une intervention dont les exclusions ne sont pas curées se propose **en le disant** — la curation partielle est un état déclaré, jamais un silence. **CADRÉE LE 2026-09-16** — `CADRAGE_EXCLUSIONS_INTERVENTIONS_2026-09-16.md`. Le cadrage nomme les **trois niveaux d'exclusion** que le routage confondait (axe / source d'intervention / document), et constate que **seul le niveau axe a un consommateur d'exécution**. L'audit du corpus fournit le sujet manquant : **61 des 95 sources déclarent des exclusions**, et **55 documents mentionnent l'un des six critères binaires de la gate** — grossesse 51, rénal 13, cœliaque 11, allaitement 11, hépatique 8, **chirurgie digestive 0**. |
| à cadrer | Les dix règles orphelines de la constitution | *(pas de dossier — routée le 2026-08-24, `D-107`)* | **Arbitrage praticien du 2026-08-24, après deux reports.** `DC-03`, `DC-36`, `DC-38`, `DC-39`, `DC-40`, `DC-41`, `DC-44`, `DC-45`, `DC-47`, `DC-48`, plus la part de `DC-11` hors exclusions. Le LOT-01 de « Doctrine exécutable » les avait nommées orphelines (`D-095`), `D-096` en a sorti `DC-09` pour le LOT-09, et l'arbitrage de portefeuille était **reporté, pas clos** — trois options nommées : dettes nommées, campagne dédiée, rattachement au coup par coup. **Le responsable tranche : campagne dédiée.** Les deux autres options sont écartées avec leur motif — « dettes nommées » était le régime qui les a rendues orphelines, et le « coup par coup » ne fait remonter aucune règle sans porteur. Le **LOT-08** de « Doctrine exécutable » les écrit comme telles ; il ne les arbitre plus, c'est fait ici. Recompte au grep avant cadrage : `grep -c '\*\*Orpheline\*\*'` rend **13** au 2026-08-24 (onze statuts + deux en en-tête). |
| à cadrer | **Moitié PRATICIEN de l'objectif : la reformulation se cite, elle ne se tape plus** — jumelle de l'entrée suivante | *(pas de dossier — **débloqué par [[D-160]]**, 2026-09-10)* | **DÉBLOQUÉ.** `D-160` ouvre la seconde liste : la reformulation se cite depuis une version **publiée** de la synthèse, jamais un brouillon, et **la garde bloquante manquante sur la saisie libre se pose avec** — l'un ne va pas sans l'autre. Le reste de cette entrée décrit le chemin, il ne s'arbitre plus. **La question posée** : « comment remplir la partie praticien de l'objectif négocié — ce que j'ai compris de votre dossier ». **Tranché : par CITATION de la synthèse publiée « Ce que j'ai compris de vous », jamais par une saisie libre assistée par la machine.** Trois raisons, par ordre de poids. **(1) LA GARDE, et c'est la raison décisive.** Publier une synthèse passe un refus BLOQUANT sur le registre anxiogène (`api/praticien/comprehension/route.ts:419-422`), dont le message dit sa propre raison : elle « est lue seule par le patient, souvent avant la consultation ». La reformulation de l'objectif, elle, n'a **aucune garde à l'écriture** (`api/praticien/objectifs/route.ts` : 0 occurrence de `termeAnxiogene`), et au portail le même contrôle tourne mais **journalise sans bloquer** (`api/portail/dossier/route.ts:454-459`, [[D-090]]). Or depuis [[D-154]] la reformulation est exactement dans la condition qui justifie la garde de l'autre : lue seule, au portail, avant la consultation suivante. **Citer un texte déjà publié est le seul chemin qui garantit qu'il a franchi le refus bloquant.** **(2) La symétrie.** L'énoncé se remplit déjà par citation — anamnèse verbatim, ou amendement du patient (`objectifs/route.ts:58-63`) ; la moitié praticien doit se remplir de même, depuis ses mots à lui déjà publiés. Règle tenable en une phrase : *chaque voix cite ce qu'elle a déjà dit ailleurs, personne ne recopie l'autre.* **(3) La place de la machine — ET ELLE N'EN A AUCUNE ICI.** `SyntheseIA` (`schema.prisma:352`, statuts `Brouillon_IA` → `Validee_Praticien`) n'entre pas dans l'objectif. **CORRECTION DU 2026-09-09, POSÉE LE JOUR MÊME** : une première rédaction de cette entrée la plaçait « un cran plus tôt, en aide à la rédaction de la synthèse de compréhension ». **C'est faux, et la garde G3 nomme ce scénario mot pour mot** : « Importer un moteur *pour suggérer* une synthèse ferait entrer une sortie de scoring dans un texte publié au patient sous la signature du praticien » (`web/src/lib/comprehensionAppendOnly.guard.test.ts:161-166`, imports fermés `:172-178`, `DC-31`/`DC-32`). Deux objections s'y ajoutent : `SyntheseIA.donneesEntree` descend des scores de questionnaires, donc lire la table depuis une autre route serait un contournement et non une conformité ; et son `narratif_patient` **part déjà au patient** dans le bilan (`lib/documents/bookletHtml.ts`), si bien qu'un pré-remplissage lui ferait lire deux fois le même texte, par deux canaux, dont un sans aucun mécanisme de contestation. La machine ne rédige donc ni l'une ni l'autre moitié. **Défaut mis au jour en tranchant, vu ni par le compte rendu ni par la contre-revue** : deux textes praticien disent « ce que j'ai compris » dans la MÊME phase 3 — reformulation (« Ce que vous avez compris de la demande… », `ObjectifNegociePanel.tsx:1217`) et synthèse (« Ce que vous me dites, tel que je l'entends… », `ComprehensionPanel.tsx:158`, `:344`) — **sans aucun lien de code** (0 occurrence de `idSynthese` dans `objectifNegocie.ts` et `objectifs/route.ts`) et **avec deux gardes inégales**. Rien n'empêche une synthèse publiée et une reformulation de se contredire ; le patient les lit sur deux écrans, avec deux verbes différents (ratifier/contester d'un côté, signaler un désaccord de l'autre). **Chemin** : décision `D-xxx` ouvrant une source citable pour la REFORMULATION (la liste fermée de [[D-094]] §1 ne régit que l'énoncé — vérifier au cadrage si elle doit être étendue ou doublée) → citation par identifiant de version publiée, recopie serveur, fragment portant `idSynthese` + `publieeLe`. **Conditions** : (a) seule une version **publiée** est citable, jamais un brouillon — sinon la garde de registre est contournée ; (b) la garde bloquante manquante sur la saisie libre se décide **avec**, sinon la citation devient le chemin sûr et la frappe le chemin sale ; (c) ne pas pré-remplir, ne pas fusionner les deux textes — portées distinctes (la demande / la personne) et verbes patient distincts. **Étage minimal, sans décision** : afficher la synthèse publiée courante à côté du champ de reformulation, comme le matériau d'anamnèse l'est déjà à côté de l'énoncé (`ObjectifNegociePanel.tsx:689`). |
| à cadrer | **Amorcer « Ce que j'ai compris de vous » par les mots du patient** — sœur des deux entrées voisines | *(pas de dossier — **aucune décision requise**, confirmé le 2026-09-10)* | **AUCUNE DÉCISION REQUISE**, et `D-160` le confirme : ce chantier ne cite que des paroles du patient déjà en base, ne franchit aucun `IMPORTS_INTERDITS`, et le panneau admet déjà le geste en révision. Seule condition : la surface entre dans `SURFACES_LOT`. **La demande** : supprimer la page blanche de la sous-vue « Ce que j'ai compris » de la phase 3. **Le pré-remplissage par la synthèse IA est ÉCARTÉ, et pas par prudence** : la garde G3 nomme le scénario mot pour mot — « Importer un moteur *pour suggérer* une synthèse ferait entrer une sortie de scoring dans un texte publié au patient sous la signature du praticien » (`web/src/lib/comprehensionAppendOnly.guard.test.ts:161-166`, `DC-31`/`DC-32`). Deux objections s'y ajoutent : `SyntheseIA.donneesEntree` descend des scores, donc déplacer la lecture dans une autre route serait un contournement ; et `narratif_patient` **part déjà au patient** dans le bilan, d'où une double lecture du même texte par deux canaux, dont un sans mécanisme de contestation. **RETENU : amorcer par les MOTS DU PATIENT, cités, jamais rédigés.** Le champ demande littéralement « Ce que vous me dites, tel que je l'entends… » (`ComprehensionPanel.tsx:344`) : ce qui y répond est ce que le patient a dit. Quatre matériaux, tous déjà en base et tous écrits par lui — (a) l'anamnèse, `motif_principal` / `objectif_prioritaire` / `attentes`, que la route des objectifs lit **déjà dans la même phase** (`api/praticien/objectifs/route.ts:341`), ce qui fait précédent ; (b) « ce qui compte pour moi aujourd'hui », daté, le plus récent ; (c) l'énoncé de l'objectif courant, lui-même recopie serveur des mots du patient ; (d) le désaccord, en révision — réviser la version contestée étant le seul geste qui le fasse passer à « répondu » (`syntheseComprehension.ts:375`). **La forme fait la doctrine** : pas un paragraphe rédigé à sa place, mais les citations posées avec date et provenance, entre lesquelles le praticien écrit. La machine cite, et ici elle n'assemble même pas : elle dispose. **Ce que cela ne franchit pas** : aucun import de `IMPORTS_INTERDITS` (`guard.test.ts:172-178`), aucune propriété de mesure ordonnée (G2), aucune nature diagnostique (G3), porte de publication intacte. **Le geste est déjà admis dans ce panneau** : « Réviser cette version » pré-remplit le texte (`ComprehensionPanel.tsx:222`) — la page blanche ne concerne que la PREMIÈRE synthèse. **Si la voie machine est rouverte malgré tout**, une décision doit établir quatre points : statut `Validee_Praticien` seul, `narratif_patient` seul et jamais `axes_prioritaires` (ordonnés, `DC-19`/`DC-20`), texte entrant **marqué comme citation** et non comme prose du praticien, et amendement EXPLICITE de `IMPORTS_INTERDITS` plutôt qu'une lecture déportée. |
| à cadrer | **Moitié PATIENT de l'objectif** : « Ce qui compte pour moi aujourd'hui » devient citable dans l'objectif négocié | *(pas de dossier — **débloqué par [[D-160]]**, 2026-09-10)* | **DÉBLOQUÉ.** `D-160` porte la liste de `D-094` §1 à quatre entrées et confirme les quatre conditions ci-dessous. **RÉSERVE D'ORDONNANCEMENT, FERME** : le bouton « Reprendre cette phrase » produit *mécaniquement* une seconde tête dès qu'un objectif courant existe (`objectifs/route.ts:755`) — le verbe de départage se livre AVANT. **Une quatrième source pour le moteur de proposition, avec le bouton « Reprendre cette phrase ».** Aujourd'hui le dépôt du patient n'est lu par aucune surface de l'objectif : `entreeCeQuiCompte` n'est consommé que par `portail/dossier`, `portail/ce-qui-compte`, `praticien/ce-qui-compte` et l'effacement, et son panneau vit dans l'onglet **Trajectoire**, pas dans la phase 3 où l'objectif s'écrit (`FichePatientPanel.tsx:1797-1799`). Le praticien qui pose un objectif ne voit donc pas ce que son patient a déposé. **Il alimente l'ÉNONCÉ, jamais la PRIORITÉ** — deux motifs, aucun n'est de confort : la priorité est l'arbitrage du praticien (« ce sur quoi on travaille d'abord »), y verser une parole de patient convertirait silencieusement l'une en l'autre, ce que le dossier à deux voix existe pour empêcher ; et elle est bornée à 200 caractères contre 4 000 pour un dépôt, donc il faudrait **tronquer** — que `lib/patient/ceQuiCompte.ts:44-50` nomme explicitement contre-patron et « ALTÉRATION DE DONNÉE ». **La garde G2 ne s'y oppose pas, vérifié** : `CONSOMMATEURS_INTERDITS` (`ceQuiCompteAntiAgregat.guard.test.ts:98-107`) ferme `lib/clinical`, `lib/clinical-engine`, `lib/scoring`, `lib/synthese-praticien.ts`, `lib/documents`, `lib/correspondance`, `lib/equilibre` et `api/praticien/synthese` — **`lib/praticien/` n'y est pas**, et citer verbatim n'est pas agréger. **Chemin** : décision `D-xxx` neuve étendant la liste FERMÉE de `D-094` §1 (`propositionObjectif.ts:7-10` : « toute extension est une décision `D-xxx` neuve, pas un champ de plus ») → `SourceCeQuiCompte` en quatrième membre de `SourceFragment` (`:94`) → fabrique marquée → la route élargit `fragment_non_citable` (422, `objectifs/route.ts:574`) au-delà de la seule nature `anamnese`, et le panneau son test de nature (`ObjectifNegociePanel.tsx:765`). **Quatre conditions non négociables** : (1) le fragment porte sa `saisiLe`, comme le fragment d'anamnèse porte sa `dateConsultation` — sans quoi on cite un « aujourd'hui » vieux de trois mois comme une demande actuelle ; (2) **un seul dépôt, le plus récent** — en citer plusieurs, c'est les ordonner, et la table n'a délibérément **pas** de `supersedes` (`ceQuiCompte.ts:21-29` : « une parole n'est pas une donnée qu'on rectifie ») ; (3) toute surface neuve rendant ces entrées entre dans `SURFACES_LOT` (`guard.test.ts:28`), faute de quoi l'anti-agrégat ne s'y applique pas — la garde le dit d'elle-même de sa dernière addition ; (4) aucun décompte, aucun résumé, aucune moyenne, y compris à l'écran. **Limite structurelle à connaître avant de cadrer** : l'assemblage ne tourne qu'à la **confirmation d'un épisode** (`ClinicalRuntimeSection.tsx:928`), donc un dépôt écrit après ne rafraîchit rien de lui-même et rien n'apparaît en première consultation. **L'étage minimal reste utile et se cadre avec** : afficher le dépôt dans le bloc d'ancrage à côté de la saisie, comme le motif principal et les attentes (`ObjectifNegociePanel.tsx:689`) — aucune décision, aucune source neuve, une lecture de plus dans `GET /api/praticien/objectifs`, relue à **chaque** entrée en phase et donc dès la première consultation. Les deux étages sont complémentaires, pas alternatifs. |
| gaté | Mémoire relationnelle consentie | *(pas de dossier — gaté conformité)* | Nouvelle finalité RGPD ⇒ information des personnes + base légale d'abord ; derrière la revue HDS du 2026-10-21. La mécanique trust (version immuable + accusé + retrait) est prête ; la table devra entrer dans l'effacement IDP2. |
| à cadrer | **Relire l'usage du protocole 21 jours après deux semaines** | `2026-09-14-protocole-assiste/lots/LOT-07-bilan.md` | **Entrée posée à la CLÔTURE du 2026-09-16, et c'est le seul moyen de savoir si neuf lots servent.** La mesure faite le jour de la clôture rend **zéro version de protocole C1 en production** — l'unique ligne de `protocol_drafts` est celle du 2026-07-31, sous un contrat d'observation alimentaire. Ce zéro mesure **un jour** de fonctionnement, pas une adoption : c'est une ligne de base. **La requête est conservée telle quelle** au dossier du LOT-07 (neuf comptages, aucun champ nominatif) — la rejouer à l'identique est la condition pour que la comparaison dise quelque chose ; la reformuler déplacerait la question entre deux lectures. **Ce qui a bougé entre le 13 et le 16 septembre** : épisodes T0 de 7 à 8, sélections de priorité de 1 à 3, protocoles de 0 à 0 — l'entonnoir franchit la phase Décision et s'arrête à la phase Actions. **Contrainte d'exécution** : le classifieur du mode auto refuse la FORME de la commande (`run -d` + `psql`), pas son contenu ; il faut une session hors mode auto. |
| à cadrer | **JA — LOT-05 : activation du protocole JA sans migration** | `2026-07-13-journal-alimentaire-21j-v1/lots/` | **Dette nommée le 2026-09-16, à la remise d'aplomb du portefeuille.** Cinq lots sur six sont terminés ; ce dernier est réellement inachevé et **sans porteur depuis le 2026-07-18**. Sa campagne passe à « suspendue » : la laisser « en cours » faisait lire du travail vivant à chaque session, deux mois durant. Le lot n'est pas requalifié ici — il est nommé, pour qu'un cadrage le reprenne ou qu'un arbitrage l'écarte. |
| à cadrer | **C4 — Compléments clean label**, jamais ouverte | `2026-07-11-complements-clean-label-v1/` | **Retournée en file le 2026-09-16** : ses cinq lots sont `à_faire`, son dernier geste au dossier date du 2026-07-24 (une étude de faisabilité de corpus). Elle n'a jamais été ouverte ; son statut « en cours » comptait une campagne active de plus dans chaque état de session. Rang à arbitrer au moment de l'ouvrir. |
| à cadrer | **Les dettes que la contre-revue du protocole laisse ouvertes** | *(pas de dossier — nommées par [[D-200]], 2026-09-16)* | **(1) FERMÉE LE 2026-09-16** (PR #1137) — la seconde description de la vue patient n'existe plus : le contenu patient est projeté par `contenuPatientProtocole.ts`, que `buildPatientProtocolView` enveloppe et signe pour la diffusion, et que l'aperçu appelle nu. Le miroir de diffusion sert cet aperçu **sur la version active**, avec la phrase d'attente des interventions non fermes et le motif des refus ; le praticien voit donc ce que son patient lira AVANT de le diffuser. Quatre dettes restent. **(2) `projeterSurLeFil` n'a aucun banc**, alors qu'il décide seul de ce qui atteint le navigateur du patient et recopie champ par champ : un champ ajouté au contrat y serait abandonné en silence, `tsc` vert — le mécanisme exact qui a fait voyager `followUpCriterion` des mois sans écran. **(3) TRANCHÉ ([[D-213]] §3) — le statut s'AFFICHE et le praticien le pose.** ~~Le statut `active` est posé en silence~~ sur les actions non suspendues, valeur que le formulaire n'affiche jamais et que `protocolDraft.ts` refuse précisément de poser par défaut (`DC-24` : « active est la valeur la plus engageante »). **(4) TRANCHÉ ([[D-213]] §2) — il SORT du contrat patient.** ~~`limitations` est projeté jusqu'au patient et rendu par aucun écran~~ — même classe que `followUpCriterion`, toujours ouverte. **(5) Hydratation du constructeur depuis la version active, et `versionsLues` non remonté au rail** — coupures de `D-179`, reconduites. |
| à cadrer | **Le protocole assisté tiré du corpus** | `CADRAGE_PROTOCOLE_DEPUIS_LE_CORPUS_2026-09-16.md` | **CADRÉE LE 2026-09-16, et un arbitrage bloque son ouverture.** Sujet né en session le jour même, sans véhicule : la campagne « 5. Actions » a été close le matin, à usage mesuré zéro, et l'assistance au remplissage des actions n'y figurait pas. **Le mécanisme d'assistance existe trois fois** — `suggererDepuisLignes` sur table signée (`D-198`), la recherche de corpus en production (`D-081`, élargie par `D-188`), le producteur d'intentions suspendues (`D-190`) — ce qui manque est la **matière**. L'audit du corpus la mesure : 123 documents portant une conduite, **575 actions couvrant les dix types** du contrat V4, 107 hiérarchies explicites. **Le constat qui commande tout le reste** : le corpus donne le plan idéal à 99 % et le plan minimal à **19 %** — or `minimalPlan` est le champ servi AU PATIENT. **A1, A2, A3 ET LE VÉHICULE SONT TRANCHÉS LE 2026-09-16 — [[D-206]]** : régime `WN-CL-0287-009` (un claim fonde l'indication, le raccourci se nomme sur place), **le tableau clinique pour unité**, le plan minimal **dérivé de l'idéal** sous trois conditions, et **pas de campagne — un seul lot borné**, passé en chantier hors file ci-dessous. Restent ouverts A4 (les 514 « à ne pas faire ») et A5 (pré-remplissage modifiable ou cité). Mesures consignées dans `MESURES_AUDIT_CORPUS_2026-09-16.md` — le fichier brut reste hors dépôt (G6 fermé). |
| à cadrer | **La Boussole alimentaire : l'assiette, l'aliment, et lequel devient une action** | `CADRAGE_BOUSSOLE_ASSIETTE_2026-09-16.md` | **CADRÉE LE 2026-09-16**, sur une réflexion posée en session : « choisir un aliment dans la Boussole comme action me paraît incongru ». **L'état réel désamorce la moitié de la question** : `attachFoodCompassRef` attache bien une référence d'aliment à une action, mais **aucune route ni composant ne l'appelle** — seul son banc le lance, et `WN_C5_ENABLED` commande son refus. Rien à défaire en production. **L'assiette a déjà son catalogue** — `C5B_RECOMMENDED_PLATES`, trois entrées, `contentHash` par entrée et hachage de catalogue — mais son unique consommateur est le panneau d'**observation**, pas le protocole. Le corpus tranche dans le même sens : **119 actions de domaine « assiette » contre 3 documents « aliment vedette » sur 123**, et le seul précédent corpus → table signée → runtime (`WN-CL-0287-009`) fonde l'indication d'une **assiette**. **Le cadrage refuse la substitution** et sépare trois rôles : l'assiette devient l'unité qui porte une action, l'aliment vedette devient le contenu d'un plan, la Boussole reste une surface de compréhension patient. Quatre arbitrages, aucun bloquant hors B1. |
| à cadrer | **~~Trois défauts vivants~~ Trois pièges ARMÉS du moteur de protocole — requalifiés par [[D-207]]** | *(pas de dossier — nommés le 2026-09-16, distincts des cinq dettes de [[D-200]])* | Trouvés par le panel de conception du protocole assisté, **écrits nulle part jusqu'ici**, et re-vérifiés au dépôt le 2026-09-16. **REQUALIFIÉS LE 2026-09-16 ([[D-207]]) : aucun des trois n'est atteignable aujourd'hui.** Le mot « vivants » était faux, et le classement qui en découlait aussi. Ce sont des **pièges armés** — ils se déclencheront au moment où quelqu'un ajoutera la surface manquante, sous un `tsc` vert, c'est-à-dire au pire moment. **(1) FERMÉ LE 2026-09-16** — `clinicalContentHash` hache désormais `phases`, tenu par quatre bancs dont un **épinglage** de l'empreinte d'un protocole sans phase : ajouter un champ à une empreinte aurait fait basculer en « changement clinique » tous les fils déjà persistés. Le scénario « le praticien perd sa saisie » n'était pas atteignable : **`ProtocolPhase` n'a ni producteur ni lecteur** — la route unique n'a jamais passé `phases`. **(2) TRANCHÉ ([[D-213]] §5) — un MOTIF par cause**, la fonction cessant de rendre `null` : « aucune ligne ne couvre », « votre barème se contredit », « barème non attesté ». Le deuxième ne se dirait jamais autrement. ~~`suggererDepuisLignes` rend un `null` ambigu~~ (`baremeChargePur.ts:114-115`), mais **il ne peut pas survenir aujourd'hui** : la table signée est une échelle sur un seul terme couvrant `(-∞,1] ∪ [2,2] ∪ [3,3]` sur un domaine borné 0 à 3 — ni trou ni recouvrement, le recouvrement étant en outre refusé par un banc de garde — et l'écran distingue lui-même les deux autres causes avant d'appeler. **Aucun correctif évident** : départager les causes demande de choisir ce que l'écran dirait dans chacune, donc un arbitrage. **(3) TRANCHÉ ([[D-213]] §2) — le champ SORT du contrat patient**, avec la dette 4 de `D-200` : un champ signé jusqu'au patient que personne n'alimente et qu'aucun écran ne rend est une promesse non tenue. ~~`patientLimitations` est un paramètre ouvert sans producteur~~ : les **quatre** appelants de production passent `[]`, et le champ `limitations` qu'il alimente **n'est rendu par aucun écran** (dette 4 de `D-200`). Un canal signé jusqu'au patient, sans émetteur ni lecteur. |
| à cadrer | **« Relu par le praticien » est un tampon posé par le SERVEUR, sans condition** | *(pas de dossier — trouvé le 2026-09-16 par la réfutation adverse du LOT-01, [[D-208]])* | **Ce n'est pas un piège armé : c'est en service.** `protocoles/versions/route.ts` pose `review: { reviewedAt: now, reviewerRole: 'practitioner', confirmation: 'content_reviewed' }` **à chaque enregistrement, sans aucune condition**. Un contenu enregistré sans une seule frappe ressort donc « relu par le praticien » et franchit la garde de diffusion. **Conséquence de portée** : aucun raisonnement de sûreté du dépôt ne peut s'adosser à « la relecture praticien répond de ce texte » — et plusieurs le font. **TRANCHÉ LE 2026-09-16 ([[D-213]] §1) : DEUX gestes.** La coche `reviewed` du constructeur — qui existe déjà, s'affiche, et se remet à faux à chaque frappe — sera TRANSMISE, et `review` vaudra `null` quand elle est fausse ; la validation pour diffusion reste le second verrou. Le transport manquait, pas la conception. **Second fait du même examen, pour le même lecteur** : le chemin le moins cher pour satisfaire les trois gardes `nonEmpty` du brouillon est de **recopier le plan idéal dans le plan minimal**, et rien ne l'attrape — ce qui pèse directement sur l'arbitrage A3 de [[D-206]]. |

Critère d'acceptation transverse des lots 6.0 (principe architectural §7 de
l'audit) : une stratégie cliniquement pertinente mais incomprise, irréalisable
ou sans rapport avec ce qui compte pour le patient n'est pas « done ».

## Chantiers en cours hors file — de l'ingénierie, pas des campagnes

Ni des campagnes (pas de dossier, pas de lots), ni des gestes du responsable :
du travail de dépôt porté par sa décision et sa PR. Ils figurent ici pour que la
file cesse de laisser croire que rien ne se fait en dehors d'elle.

- **`D-206` — la première table du catalogue de conduites** (2026-09-16,
  arbitrage du responsable, **non commencé**). Le catalogue n'ouvre **pas** de
  campagne : `D-112` pèse sur trois campagnes de suite et la dernière a été
  close à usage mesuré zéro. **Un seul lot**, une PR — la forme d'une ligne
  (clé de tableau clinique, champs, `claimId` fondant l'indication, emplacement
  du raccourci assumé) **et** une première table. **L'axe de départ n'est pas
  un choix de sujet, c'est la conséquence mécanique de A1** : exiger un claim
  par ligne ferme les cinq axes non curés, et il ne reste que **sommeil** (297
  claims validés) ou **humeur** (283). **Deux choses se font dans cet ordre** :
  normaliser les tableaux cliniques — le corpus les porte en prose libre, et
  les compter compterait des rédactions — PUIS dimensionner la table, qui doit
  être **petite ET stable** puisqu'un périmètre signé se hache en entier.
  Cadrage : `CADRAGE_PROTOCOLE_DEPUIS_LE_CORPUS_2026-09-16.md`.
  **LOT-01 LIVRÉ LE 2026-09-16 ([[D-208]])** — `catalogueConduitesV1.ts` :
  la forme d'une ligne, un verrou à SIX termes, un service fail-closed, et la
  table **VIDE**. La surface de relecture
  (`SURFACE_RELECTURE_CATALOGUE_CONDUITES_2026-09-16.md`) propose **trois lignes**
  sur le bloc sommeil et écarte **huit tableaux** avec leur motif. **Ce qui reste
  au responsable** : désigner les `claimId` qui fondent chaque indication — le
  dépôt sait qu'il y en a 20, 29 et 7 de validés sur les trois sources, il ne sait
  pas lesquels.

- **`D-113` — les cycles nommés `T0`, `T1`, `T2`** (2026-08-26, arbitrage du
  responsable). Chaque cycle s'ouvrait par un `T0` : un second cycle
  **déplaçait l'ancre du premier** et refermait ses fenêtres de jalon par effet
  de bord. **PR 1 mergée** (#803) — la structure : série d'ancres ouverte,
  `JOURS_JALON` re-typée sur les seuls jalons de mesure (un `Record` indexé par
  un type ouvert dégénérait en signature d'index, rendant `undefined` sous un
  type `number`), garde `G7` portée. **Elle ne change aucun comportement.**
  **PR 2 mergée** (#805, 2026-08-27) — le comportement : les sites `milestone ===
  'T0'` relus un par un et tranchés entre « l'ancre du cycle courant » et « la
  toute première mesure » ; le rideau `D-052` étendu à **toute ancre** (ouvrir
  un deuxième cycle est le même acte, la clé s'élargit, aucun seuil ne bouge) ;
  **trois défauts muets supprimés** — deux `Record<JalonMomentum, string>`
  dégénérés en signature d'index (`LABEL_JALON['T1']` = `undefined` typé
  `string`), six listes littérales `['T0', 'J21', 'J42', 'J90']` qui rejetaient
  un `T1` par un `continue`, cinq requêtes `where: { milestone: 'T0' }` +
  « la plus récente » dont les deux moitiés étaient fausses. **L'ouverture d'un
  `T1` est un geste praticien nommé**, et la fermeture des fenêtres du cycle
  précédent est annoncée avant le geste au lieu d'être un effet de bord (§8).
  **Trois gardes d'écriture neuves** aux points de persistance : la forme (ni
  ancre ni mesure ⇒ refus), le rang (ancre déjà posée, ou la suivante — un
  `T7` sur un dossier qui n'a que `T0` laisserait six rangs vides) et la
  cohérence interne (identifiant et jalon déclaré qui se contredisent ⇒ refus,
  au lieu d'un arbitrage muet qui laissait ouvrir une ancre sans rideau).
  **La revue P0 a rendu NO-GO sur la première version de cette PR**, et le
  correctif décisif touche l'identité des épisodes : `runtime-episode-<patient>-<jalon>`
  n'était unique que tant qu'un dossier n'avait qu'un cycle. Le `J21` du second
  y prenait la même **clé primaire** que celui du premier, et la persistance
  écrit par `upsert(..., update: {})` — la confirmation n'écrivait rien sous
  une réponse `ok: true`. Le `T1` que cette décision permet d'ouvrir aurait été
  un cycle dont aucune mesure n'était confirmable. Un jalon de mesure porte
  désormais le nom de l'ancre de son cycle. **`D-113` est close.**
  **Dette reconduite** : `assessment_episodes.milestone` reste une colonne
  `String` **sans CHECK** — les gardes ci-dessus la couvrent au bord
  applicatif, la contrainte en base est une migration à part, avec sa
  confirmation distincte. Fenêtre favorable inchangée : la table est **vide en
  production**, donc aucune donnée à migrer ; le premier `T0` confirmé rendrait
  la bascule payante.

## Ce qui n'est PAS en file — des gestes, pas des campagnes

- **La messagerie de santé (MSSanté et consorts) est HORS DE PORTÉE** — écrit une
  fois pour toutes par [[D-220]] §4, parce que la question revient à chaque cadrage
  du fil médecin. Trois raisons, stables et indépendantes d'un arbitrage à venir :
  l'application n'a **aucun canal sortant** vers un médecin ; l'opérateur de
  messagerie serait un **sous-traitant de plus** à contractualiser, sur des données
  de santé ; et l'identité du médecin **n'est pas authentifiée** ([[D-219]] §1,
  option C). Ce n'est pas un lot repoussé, c'est un **périmètre fermé** — il ne se
  rouvre que par une décision explicite du responsable.
- **Le registre TRUST patient promet une garde que le logiciel ne tient pas** —
  nommé par [[D-220]], **non touché**. Quatre versions publiées portent « aucun
  partage avec un tiers … sans un choix explicite de votre part », et l'effet du
  refus dit « aucun document ne sera partagé » ; [[D-219]] §3 confie pourtant la
  règle au praticien, informé. Ce sont des documents **publiés, versionnés et
  immuables**, sur lesquels des patients ont consenti : les corriger demande une
  **version neuve** et un arbitrage du responsable. Le geste n'est pas technique.
- **La table des signaux de sécurité est invisible à l'audit de consommation** —
  constat de la revue de `D-218`, **antérieur au lot** et routé par lui.
  `SOURCES_DE_SAVOIR` (`scripts/wn-matrice-consommation.mjs`) déclare l'inventaire
  qui construit `docs/claude/MATRICE_CONSOMMATION.md`, et il y liste les autres
  tables cliniques signées — mais **pas** `lib/clinical/safetySignalsV1.ts`, dont
  les conduites, le SHA et le verrou de signature sont consommés par le cockpit
  depuis `D-099`, et désormais par la lettre d'adressage. Ses consommateurs
  n'apparaissent donc nulle part dans l'audit. Le geste : déclarer la source, dire
  ce qu'elle sert et sous quels drapeaux, régénérer la matrice — et vérifier au
  passage si d'autres tables signées manquent à l'appel. Ce n'est pas une
  campagne : c'est une déclaration d'inventaire, et elle a sa propre finalité.
- **La première ligne du barème de charge** — `2026-09-14-protocole-assiste`,
  LOT-06. Le **mécanisme est écrit et ses dix bancs sont verts** (table signée à
  quatre termes, refus sur table vide, refus de la discordance) ; la table est
  **vide**, et rien ne partira sans au moins une ligne écrite et signée par le
  praticien. `DC-19`/`DC-20` interdisent d'inventer une borne, et la campagne a
  mesuré ce que devient une table livrée vide : `clinical_rules`, les alertes
  compléments et les seuils d'ingrédient portent **0 ligne chacune**. La surface de
  relecture — les quatre termes mesurés, la forme d'une ligne, ce que le mécanisme
  refuse — est écrite au dossier du lot.
- **Mesurer l'usage de la campagne « 5. Actions »** — sur dossiers réels, par
  identifiant, au conteneur (`D-125`), jamais sur fixture. Elle devait être faite au
  LOT-07 et a été **refusée par le classifieur de sécurité** de la session du
  2026-09-15 ; elle n'a pas été contournée. Ce que la campagne a livré est vert en CI
  et constaté en ligne par contenance — son **usage n'est pas mesuré**, et c'est
  exactement l'avertissement que le dossier portait à l'ouverture.
- **`adviceSheetRef` est mort de bout en bout** — dette nommée par `D-191`
  (LOT-03 de `2026-09-14-protocole-assiste`), non refermée par lui. Le champ
  existe au contrat de vue patient, la route l'écrit `null` depuis toujours, et
  **aucun champ du constructeur ne le renseigne**. Le bouton qui promettait « ma
  fiche conseils » dit désormais ce qu'il fait — il mène au centre TRUST —, mais
  produire une vraie fiche conseil est une **surface neuve**, pas une correction :
  elle appelle sa propre décision et son propre lot. Porteur : aucun à ce jour.
- **Aucune limitation patient n'est servie par le protocole** — même décision.
  Celles de la carte sont écrites POUR LE PRATICIEN (« aucune priorité ne peut
  être proposée avant… ») : les traduire serait fabriquer du texte patient, les
  recopier lui servir un raisonnement interne. Le champ reste au contrat, vide et
  nommé.
- **Relancer le recueil 21 jours** du carnet alimentaire (pilote PAT006) :
  seul débloqueur des campagnes existantes `2026-08-04-agenda-alimentaire`
  (LOT-06 barème) et `2026-08-10-chaine-alimentaire` (LOT-02/03) — elles
  reprennent, elles ne se recréent pas. Indépendant de tout l'axe 6.0.
- **Orientation NNPP2 — FAIT, plus rien à signer ni à poser** : les trois
  tables sont signées depuis les 2026-08-15/16 (`D-061`, `D-062`, `D-067`,
  frein structurel `D-065`), les deux drapeaux sont posés, et l'activation
  est CONSTATÉE en production — par le comportement (`D-074`, 2026-08-18 :
  le panneau sert des recommandations, seule preuve valable puisque la
  variable est sensitive et que les E2E arment le même drapeau sur la même
  base), puis valeur `'1'` confirmée au panneau le 2026-08-19. Reste la
  dette d'interface nommée par `D-074` §4 : `MESSAGE_ORIENTATION_INACTIVE`
  périmé (ne s'affiche plus en production, correction = autre finalité).
- **Constater** la proposition de bilan sur un vrai dossier (preuve terminale
  de la chaîne T0).
- ~~**Désarmer `WN_CB_RESULTS_ENABLED`**~~ — **renversé le jour même par
  `D-078`** : le responsable autorise explicitement l'activation de l'étage 2
  (résultats réels), le désarmement n'est plus la consigne et le drapeau
  reste posé. Trace conservée : le relevé du matin (variable à `true` contre
  son verrou « jamais avant l'attestation HDS ») était exact — c'est la règle
  qui a changé, par écart assumé. Dette résiduelle nommée : le commentaire du
  verrou (`biology-library/featureFlag.ts`, « GATE DUR HDS : ne doit jamais
  passer à true avant l'attestation ») contredit désormais `D-078`.
- ~~**Dette nommée, portée hors rature** : le commentaire du verrou
  `isCbResultsEnabled` (`web/src/lib/biology-library/featureFlag.ts`, « GATE
  DUR HDS : ne doit jamais passer à true avant l'attestation d'hébergement »)
  contredit `D-078` depuis le 2026-08-19 — à réviser à l'ouverture de CB-09,
  qui est précisément le moment où ce commentaire sera lu comme une
  garantie.~~ — **Soldée, constatée le 2026-09-04** : le commentaire porte
  désormais la requalification `D-081` (« hébergement HDS effectif et
  exclusif ») et les appelants `D-122` §2 — révisé par les PR de CB-09
  (2026-09-03), la file ne l'avait pas consigné.
- **Filtre de validité des passations — FAIT, allumé le 2026-08-19**
  (`D-077`, arbitrage praticien en session) : `WN_ENABLE_VALIDITE_PASSATIONS=1`
  posé et porté par un redéploiement aliasé. Geste sûr, prouvé avant
  l'arbitrage : 111 passations en production, toutes `VALID` — aucun calcul
  ne change, la route d'invalidation praticien s'ouvre (fin du 503).
  Vérification restante à l'œil : le geste d'invalidation répond sur un
  dossier de test réel.
- **Trancher les arbitrages pendants** : `complements-clean-label-v1`
  (« remplacée ? »), dégel JA5-05, ~~sort de `2026-08-02-rayon-biologie-cb`
  (recouverte par le LOT-06 de la chaîne T0)~~ — **tranché le 2026-09-04** :
  campagne close, LOT-00→04 requalifiés fiche par fiche, seul reste éventuel
  (contrat V4) transféré à l'entrée « producteur d'intentions » ci-dessus —,
  worktree `arbitrage-boucle-clinique`.

## Écarté à cet arbitrage — et pourquoi

- **Arc espace patient (E3→E2→E4)** : le dashboard E4 n'est plus un arc
  séparé — il est **absorbé** par 6.0-A/B/C, dont les écrans portail sont ses
  tranches à valeur immédiate. IDP2 (LOT-04, jeton permanent) reste bloqué
  par une mesure d'usage (12/13 accès sans nouveau chemin).
- **E8 / résultats biologiques réels et D5 / messagerie** : derrière le gate
  HDS — l'annexe signée est leur préalable, pas leur début. **Périmé pour E8
  depuis le 2026-09-03** : le gate est levé (annexe signée `D-121`,
  hébergement exclusif `D-080`/`D-120`), l'étage 2 est **livré** derrière
  `WN_CB_RESULTS_ENABLED` éteint (`D-122` §2, PR #838/#854) ; la levée du
  drapeau est un geste d'exploitation conditionné au registre des
  traitements et à l'information patient D'ABORD. D5 reste écartée telle
  quelle.
- **Programme corpus (gates G0-G4, pilote sommeil)** : G6 jamais ouvert ;
  aucune campagne tant que l'extraction du stock n'est pas faite.
