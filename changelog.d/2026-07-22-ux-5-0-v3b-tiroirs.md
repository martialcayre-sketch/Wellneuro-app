### Refonte visuelle 5.0 — lot V3b, instruments & tiroirs (2026-07-22)

Les tiroirs d'instruments du cockpit prennent l'anatomie de la maquette :
pane de 440 px (dérogation large conservée pour « Détail des réponses »,
tableau dense), ombre `shadow-pop`, eyebrow « Instrument » en encre solaire,
titre display 19 px, bouton de fermeture 40 px. `ScoreZones` s'aligne sur la
needrow maquette (piste 8 px, zones à ,14, point 14 px primaire) et gagne une
largeur paramétrable ; la couverture des 12 besoins s'affiche sur une piste
neutre **sans zones** — la couverture n'a pas de bornes de référentiel, aucun
seuil n'est suggéré, le chiffre reste affiché. Les jauges d'objets cliniques
passent en 2 colonnes fixes (une pane de 440 px ne déclenche pas les
breakpoints viewport).

Écarté : un composant `NeedTrack` séparé — `ScoreZones` portait déjà la
sémantique (zones aux vraies bornes, point plein/creux), il est restylé
plutôt que dupliqué.
